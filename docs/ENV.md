# Environment variables

Generated by `node scripts/env-doc.js` from the variables the code reads; `node scripts/env-doc.js --check` fails when this file, `.env.example` or `frontend/.env.example` drifts. Descriptions live in `scripts/env-doc.meta.json`.

Backend variables: 248. Frontend build-time variables: 23.

## Server

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `APIURL` | yes |  | Public base URL of this API, with a trailing slash (used in emails, OAuth callbacks and share links). | `Config/config.js`, `Config/contentSecurityPolicy.js` +7 |
| `APP_NAME` |  | `Alian Hub` | Product name shown in emails and page titles. | `Config/config.js`, `Modules/Template/forgotPassword.js` +5 |
| `BUILD_INFO_GIT_TIMEOUT_MS` |  | `10000` | Per-command timeout, in ms, for the git calls that resolve the running version. On timeout the server reports channel unknown and retries in the background. | `scripts/build-info.js` |
| `NODE_ENV` |  | `development` | development \| production | `Config/config.js`, `Config/cookies.js` +9 |
| `PORT` | yes | `4000` | TCP port the HTTP server listens on. | `Config/config.js` |
| `SERVER_HEADERS_TIMEOUT_MS` |  | `66000` | How long the HTTP server waits for a full request header block; always kept above the keep-alive timeout (default 66000). | `Modules/Agents/engine/timeouts.js` |
| `SERVER_KEEP_ALIVE_TIMEOUT_MS` |  | `65000` | Idle keep-alive timeout of the HTTP server; keep it above the idle timeout of any proxy in front (default 65000). | `Modules/Agents/engine/timeouts.js` |
| `SERVER_REQUEST_TIMEOUT_MS` |  | `300000` | How long the HTTP server waits to receive a whole request (default 300000). Server-sent event streams are exempt. | `Modules/Agents/engine/timeouts.js` |
| `TRUST_PROXY` |  | `loopback` | Express trust proxy setting; loopback by default so X-Forwarded-For is honoured behind a local reverse proxy. | `index.js` |
| `UNDER_MAINTENANCE` |  | `false` | true \| false | `Config/config.js` |
| `WEBURL` |  | `http://localhost:4000` | Public base URL of the web app, used in emails and CORS. | `Config/config.js`, `Config/contentSecurityPolicy.js` +3 |

## Database

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `MONGO_CONNECT_TIMEOUT_MS` |  | `60000` | How long a new MongoDB connection may take to establish (default 60000). | `Modules/Agents/engine/timeouts.js` |
| `MONGO_MIN_POOL_SIZE` |  |  | Minimum connections kept open per company database. | `utils/mongo-handler/mongoConnector.js` |
| `MONGO_POOL_SIZE` |  |  | Maximum connections per company database. | `utils/mongo-handler/mongoConnector.js` |
| `MONGO_SERVER_SELECTION_TIMEOUT_MS` |  | `60000` | How long the MongoDB driver waits to find a usable server before failing an operation (default 60000). | `Modules/Agents/engine/timeouts.js` |
| `MONGO_SOCKET_TIMEOUT_MS` |  | `300000` | Inactivity timeout on a MongoDB socket before it is closed (default 300000). | `Modules/Agents/engine/timeouts.js` |
| `MONGO_WAIT_QUEUE_TIMEOUT_MS` |  |  | How long a query waits for a free connection before failing. | `utils/mongo-handler/mongoConnector.js` |

## Auth

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `JWT_ALGORITHM` |  | `HS256` | Algorithm used to sign JWTs. | `Config/jwt.js`, `Modules/Auth/helpers/refreshTokenRules.js` +1 |
| `JWT_EXP` |  | `24h` | Access token lifetime (jsonwebtoken duration string). | `Config/jwt.js`, `Modules/Auth/controller/loginSession.js` +3 |
| `JWT_SECRET` (secret) | yes | `your_jwt_secret_here` | Secret used to sign JWTs and derive encryption keys. | `Config/instanceSettings.js`, `Config/jwt.js` +6 |
| `MAGIC_LINK_ENABLED` |  | `true` | Set to false to disable passwordless magic-link sign-in. | `Modules/Auth/controller/magicLink.js` |
| `REFRESH_TOKEN_REUSE_GRACE_SECONDS` |  | `10` | Seconds after a refresh token is rotated during which presenting the old one again is refused without revoking the session, so parallel requests or tabs do not log the user out. Past this window a reused token revokes the session. | `Modules/Auth/helpers/refreshTokenRules.js` |
| `SESSIONEXPIREDTIME` |  | `172800` | Idle time after which a login session expires. | `Modules/Auth/controller/loginSession.js`, `Modules/Auth/helpers/refreshTokenRules.js` +3 |
| `SESSION_COOKIE_HTTPONLY` |  |  | Set to on to mark the session cookies httpOnly so page JavaScript can no longer read them: the server reads the access token from the cookie when no Authorization header is sent, the socket handshake and the refresh endpoint do the same, and the web app authenticates by cookie instead of copying tokens into headers. Tracker, CLI and API-token clients are unaffected (they send headers and bodies, never cookies). Unset or off keeps readable cookies as before. | `Config/cookies.js` |
| `TRACKER_PKCE_LEGACY_UNTIL` |  |  | ISO date from which desktop tracker builds that sign in without a PKCE verifier are refused. Unset (the default) keeps those builds working; an unparseable value reads as unset. | `Modules/Auth/helpers/trackerCode.js` |
| `TWO_FACTOR_ENC_KEY` (secret) |  |  | key material for encrypting TOTP secrets at rest | `Modules/Auth/helpers/twoFactorRules.js` |
| `TWO_FACTOR_ISSUER` |  | `AlianHub` | label shown in the user's authenticator app | `Modules/Auth/helpers/twoFactorRules.js` |
| `TWO_FACTOR_TEMP_SECRET` (secret) |  | `${process.env.JWT_SECRET || ''}::2fa-temp` | signs the short-lived login second-step (temp) token | `Modules/Auth/helpers/twoFactorRules.js` |

## Mail

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AWS_SES_FROM_DEFAULT` |  |  | Default From address when mail is sent through AWS SES. | `Config/config.js` |
| `AWS_SES_KEY` (secret) |  |  | AWS access key for SES mail delivery. | `Config/aws.js`, `Config/config.js` |
| `AWS_SES_REGION` |  |  | AWS region of the SES account. | `Config/aws.js`, `Config/config.js` |
| `AWS_SES_SECRET` (secret) |  |  | AWS secret key for SES mail delivery. | `Config/aws.js`, `Config/config.js` |
| `EMAIL_IN_DOMAIN` |  | `inbox.alianhub.com` | Domain of the inbound-email addresses that create tasks. | `Modules/EmailIn/controller.js` |
| `NODEMAILER_ALLOW_SELF_SIGNED` |  |  | Accept self-signed certificates from the SMTP server. | `Modules/service.js` |
| `NODEMAILER_EMAIL` |  | `your@email.com` | SMTP username / From address. | `Config/config.js` |
| `NODEMAILER_HOST` |  | `smtp.gmail.com` | SMTP host. | `Config/config.js`, `Modules/Instance/controller.js` |

## Storage

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `CLOUD_STORAGE_ENC_KEY` (secret) |  |  | Key used to encrypt cloud-storage provider credentials at rest. | `Modules/CloudStorage/helpers/cloudCrypto.js` |
| `IAM_ENDPOINT` |  | `https://iam.wasabisys.com` | Wasabi IAM endpoint used when provisioning per-company storage users. | `Config/aws.js` |
| `S3_CONNECTION_TIMEOUT_MS` |  |  | Connection timeout for S3-compatible storage calls. | `Config/config.js` |
| `S3_SOCKET_TIMEOUT_MS` |  |  | Socket timeout for S3-compatible storage calls. | `Config/config.js` |
| `STORAGE_TYPE` | yes | `wasabi` | wasabi \| server. With server, uploads are served from the app's own origin, and whatever CSP_MODE is: every upload response carries X-Content-Type-Options: nosniff and a sandboxing Content-Security-Policy; images (including SVG), PDF, plain text, audio and video still preview inline; every other file (scripts, HTML, XML, unknown types) downloads as application/octet-stream. | `Config/config.js`, `Config/contentSecurityPolicy.js` +30 |
| `TRACKSHOT_RETENTION_DAYS` |  | `180` | Days to keep time-tracker screenshots before deletion. | `Modules/storage/server/helpers/bucket.helper.js`, `Modules/storage/wasabi/controller.js` |
| `USERPROFILEBUCKET` |  | `your-bucket-name` | Bucket that holds user profile pictures. | `Config/aws.js`, `Config/config.js` +1 |
| `WASABIENDPOINT` |  | `https://s3.wasabisys.com` | Wasabi S3 endpoint. | `Config/aws.js`, `Config/contentSecurityPolicy.js` |
| `WASABI_ACCESS_KEY` (secret) |  |  | Wasabi access key. | `Config/aws.js` |
| `WASABI_REGION` |  | `us-east-1` | Wasabi region. | `Config/aws.js` |
| `WASABI_SECRET_ACCESS_KEY` (secret) |  |  | Wasabi secret access key. | `Config/aws.js` |
| `WASABI_USERID` |  |  | Wasabi account user id used for IAM policies. | `Config/aws.js` |

## Ai

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AGENT_ACT_BUDGET_MS` |  | `300000` | Wall-clock budget for the act phase of an agent run, added to the model timeout when sizing the job lock (default 5 min). | `Modules/Agents/engine/timeouts.js` |
| `AGENT_PERFORMANCE_READ` |  | `off` | The performance.read agent action: "on" registers it and offers it over MCP, so an agent can read logged time, estimate against actual, sprint velocity and cumulative flow for up to 5 projects over at most 120 days, as the person behind the agent and with each query and its numbers kept in the replay record; default "off" leaves the registry and the MCP tool list as they were. | `Modules/Agents/performanceFlag.js` |
| `AI_API_KEY` (secret) |  |  | API key for the configured LLM provider (LLM_PROVIDER). | `Config/config.js` |
| `AI_MAX_TASK_MINUTES` |  |  | Upper bound, in minutes, for a task the AI project generator may plan. | `Modules/AIProjectGenerator/planRules.js` |
| `AI_MODEL` |  | `gpt-4.1` | recommended: gpt-4o or gpt-4.1 for best instruction-following on complex plans | `Config/config.js` |
| `AI_MODEL_ROUTER` |  | `off` | model router: "on" lets a caller pick the provider and model per call and lets the workspace routing policy name a model per task class; default "off" keeps the single configured provider answering everything, so a saved policy changes nothing | `Modules/AICore/llmProvider/normalise.js` |
| `AI_REPLAY` |  | `agent` | Which model calls keep a replay record (prompt, raw response, model, revisions; redacted): off, agent (agent runs only) or all. | `Modules/AICore/replay.js` |
| `AI_REPLAY_RETENTION_DAYS` |  | `30` | Days a replay record is kept before the TTL index deletes it. | `Modules/AICore/replay.js` |
| `AI_RESERVATION_TTL_MS` |  | `900000` | How long a budget reservation holds a workspace's money before it is treated as stranded and stops counting; a process killed mid-call leaves one behind. | `Modules/AICore/reservation.js` |
| `AI_ROUTER_BACKOFF_MAX_MS` |  | `8000` | ceiling for one backoff wait, and for a retry-after hint the provider returns | `Modules/AICore/llmProvider/router.js` |
| `AI_ROUTER_BACKOFF_MS` |  | `500` | base delay for the jittered exponential backoff between retries of a transient provider error | `Modules/AICore/llmProvider/router.js` |
| `AI_ROUTER_BREAKER_COOLDOWN_MS` |  | `30000` | how long a breaker stays open before it half-opens for one probe; doubles on each failed probe | `Modules/AICore/llmProvider/health.js` |
| `AI_ROUTER_BREAKER_MAX_COOLDOWN_MS` |  | `300000` | ceiling for the doubling breaker cooldown | `Modules/AICore/llmProvider/health.js` |
| `AI_ROUTER_BREAKER_THRESHOLD` |  | `0.5` | share of provider-caused failures in the window that opens the breaker (0..1) | `Modules/AICore/llmProvider/health.js` |
| `AI_ROUTER_BREAKER_VOLUME` |  | `5` | calls needed in the health window before the circuit breaker may open, so one bad call cannot trip it | `Modules/AICore/llmProvider/health.js` |
| `AI_ROUTER_DEFAULT_RPM` |  | `0` | requests per minute for a provider with no entry in AI_ROUTER_RATE_LIMITS; 0 means no bucket | `Modules/AICore/llmProvider/rateLimit.js` |
| `AI_ROUTER_HEALTH_SAMPLES` |  | `50` | most recent calls kept per provider and model in the health window | `Modules/AICore/llmProvider/health.js` |
| `AI_ROUTER_HEALTH_WINDOW_MS` |  | `300000` | how far back the per-provider health window looks when computing success rate, latency and the breaker | `Modules/AICore/llmProvider/health.js` |
| `AI_ROUTER_MAX_ATTEMPTS` |  | `2` | attempts on one provider before the router fails over to the next candidate (router flag on only) | `Modules/AICore/llmProvider/router.js` |
| `AI_ROUTER_RATE_LIMITS` |  |  | per-provider request-per-minute budgets as token buckets, e.g. "openai:500,anthropic:200"; empty means no bucket | `Modules/AICore/llmProvider/rateLimit.js` |
| `AI_ROUTER_RATE_WAIT_MS` |  | `2000` | longest the router waits for a rate-limit token before taking the next candidate instead | `Modules/AICore/llmProvider/rateLimit.js` |
| `ANTHROPIC_API_KEY` (secret) |  |  | Anthropic API key, used when LLM_PROVIDER is anthropic. | `Modules/AICore/llmProvider/anthropicProvider.js`, `Modules/AICore/providerKeys.js` |
| `ANTHROPIC_MODEL` |  | `claude-sonnet-4-5-20250929` | current Sonnet 4.5 model id | `Modules/AICore/llmProvider/anthropicProvider.js` |
| `ANTHROPIC_TIMEOUT_MS` |  | `600000` | optional: override the Anthropic SDK timeout (default LLM_TIMEOUT_MS, 10 min); the agent job lock is derived from the largest provider timeout | `Modules/Agents/engine/timeouts.js` |
| `DEEPSEEK_API_KEY` (secret) |  |  | DeepSeek API key, used when LLM_PROVIDER is deepseek. | `Config/config.js` |
| `DEEPSEEK_BASE_URL` |  | `https://api.deepseek.com` | optional: override DeepSeek base URL (e.g. for a compatible proxy) | `Modules/AICore/llmProvider/deepseekProvider.js` |
| `DEEPSEEK_MODEL` |  | `deepseek-v4-flash` | deepseek-chat / deepseek-reasoner / deepseek-v4-flash / deepseek-v4-pro (all V4: 1M ctx, up to 384K output) | `Config/config.js` |
| `DEEPSEEK_TIMEOUT_MS` |  | `600000` | optional: override the DeepSeek axios timeout (default: LLM_TIMEOUT_MS for reasoner, 4 min for chat); the agent job lock is derived from the largest provider timeout | `Modules/Agents/engine/timeouts.js` |
| `ESTIMATE_SAMPLES` |  |  | Number of historical tasks sampled when the AI estimates effort. | `Modules/EstimatedTime/aiTaskEstimator.js` |
| `ESTIMATE_TARGET` |  |  | Target accuracy band the AI estimator calibrates against. | `Modules/EstimatedTime/aiTaskEstimator.js` |
| `GOOGLE_API_KEY` (secret) |  |  | Google AI Studio API key, used when LLM_PROVIDER is google. | `Modules/AICore/llmProvider/googleProvider.js`, `Modules/AICore/providerKeys.js` |
| `GOOGLE_BASE_URL` |  | `https://generativelanguage.googleapis.com` | optional: override the Google generative-language base URL (e.g. for a compatible proxy) | `Modules/AICore/llmProvider/googleProvider.js` |
| `GOOGLE_MODEL` |  | `gemini-3-flash` | Gemini model id, e.g. gemini-3-flash / gemini-3-pro / gemini-2.5-flash | `Modules/AICore/llmProvider/googleProvider.js` |
| `GOOGLE_TIMEOUT_MS` |  | `600000` | optional: override the Google axios timeout (default: LLM_TIMEOUT_MS for thinking models, 4 min for the rest) | `Modules/Agents/engine/timeouts.js` |
| `KNOWLEDGE_AGENT_MEMORY` |  | `off` | Agent memory in the knowledge index: "off" publishes and indexes nothing, so agent notes stay in the LangGraph store only; "on", for a company whose knowledge indexer is on, indexes each agent note (agent_memory, namespace agent/<agentId>/note) into knowledge_chunks under scope "agent" off the memory events, backfills existing notes in the knowledge.memory.backfill job, and lets an agent's own runs retrieve its notes once that backfill is complete: only within the run's visible set (the starter's projects, narrowed to the agent's), only when every project the note was formed in is in that set, ranked below what people wrote, and with origin "external" when a tainted run formed it. People asking through Ask never see agent notes. | `Modules/Knowledge/memory/flag.js` |
| `KNOWLEDGE_ATLAS_CANDIDATE_RATIO` |  | `10` | With KNOWLEDGE_VECTOR_STORE=atlas, how many candidates Atlas considers for each chunk it returns (numCandidates is at least this times the limit, at most 10000). | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_ATLAS_INDEX_BUDGET_MS` |  | `30000` | With KNOWLEDGE_VECTOR_STORE=atlas, how long, in milliseconds, the knowledge.backfill job gives one tenant's index check before moving on; tenants are checked four at a time. | `Modules/Knowledge/vectorStore.js` |
| `KNOWLEDGE_ATLAS_INDEX_TIMEOUT_MS` |  | `5000` | With KNOWLEDGE_VECTOR_STORE=atlas, how long, in milliseconds, each step of a tenant's index check (listing, creating or updating the search index, reading a stored vector) may take before the check records the index state as timed out. Questions never wait on a check. | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_ATLAS_MAX_TIME_MS` |  | `3000` | With KNOWLEDGE_VECTOR_STORE=atlas, how long, in milliseconds, one vector search may run before the question is answered from the lexical side (the backend then reads "lexical (vector store timed out)"). | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_ATLAS_NUM_CANDIDATES` |  | `1000` | With KNOWLEDGE_VECTOR_STORE=atlas, the fewest nearest chunks Atlas considers per source type for a question ($vectorSearch numCandidates); raised to KNOWLEDGE_ATLAS_CANDIDATE_RATIO times the limit when that is more, and at most 10000. | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_ATLAS_THIN_RESULTS` |  | `3` | With KNOWLEDGE_VECTOR_STORE=atlas, when Atlas returns a full limit of chunks but fewer than this many passages survive the access check, the answer's backend reads "lexical+vector-atlas(thin)", so a vector side crowded out by chunks the asker may not see is visible. | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_ATLAS_VECTOR_LIMIT` |  | `100` | With KNOWLEDGE_VECTOR_STORE=atlas, how many chunks per source type Atlas returns for a question ($vectorSearch limit) before they are narrowed by access and collapsed to one passage per source; never fewer than the passages asked for. | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_EMBEDDING_DIMENSIONS` |  |  | With KNOWLEDGE_VECTOR_STORE=atlas, the vector size the Atlas index is built for. Leave unset to take it from a stored vector of KNOWLEDGE_EMBEDDING_MODEL, or from the model (1536 for text-embedding-3-small, 3072 for text-embedding-3-large); set it for any other model. A value that disagrees with a stored vector of the model, or with the model's known size, builds no index: the console shows the index as the wrong size and questions stay lexical. | `Modules/Knowledge/adapters/atlas.js` |
| `KNOWLEDGE_EMBEDDING_MODEL` |  | `text-embedding-3-small` | The OpenAI embedding model a hybrid company's chunks and questions are embedded with. It must have a price on file (text-embedding-3-small and text-embedding-3-large do; LLM_PRICING adds another); a change re-embeds each chunk on its next write and, a batch per run, from the recurring knowledge.backfill job. | `Modules/Knowledge/embeddings.js` |
| `KNOWLEDGE_EMBED_QUERY_TIMEOUT_MS` |  | `2000` | How long a question waits for its embedding before Ask answers from the lexical side alone (the backend then reads "lexical (embed timed out)"). Embedding at ingest keeps the provider's own timeout. | `Modules/Knowledge/embeddings.js` |
| `KNOWLEDGE_FIGURES_MAX_TIME_MS` |  | `10000` | How long, in milliseconds, the database may spend reading one workspace's knowledge figures for the instance console before the console says the figures timed out. | `Modules/Knowledge/figures.js` |
| `KNOWLEDGE_FILE_MAX_BYTES` |  | `10485760` | The largest task attachment, in bytes, whose text the knowledge indexer extracts. A larger file is recorded as skipped and never read. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_MAX_CHARS` |  | `200000` | How many characters of one file's text the knowledge indexer keeps; the rest is cut and a truncation marker added. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_MAX_PAGES` |  | `200` | How many pages of a PDF the knowledge indexer reads. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_MAX_PARSE_MEMORY_BYTES` |  | `2 x KNOWLEDGE_FILE_MAX_UNZIPPED_BYTES + 134217728` | How much memory one file's parser thread may hold (its heap plus off-heap buffers, measured by the thread itself where the parser yields, with the process's resident growth watched as a backstop for a parser that never yields) before the knowledge indexer stops it and records the file for a retry. By default twice KNOWLEDGE_FILE_MAX_UNZIPPED_BYTES (the rebuilt archive plus one entry being copied into it) plus 128 MB for the parser. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_MAX_ROWS` |  | `5000` | How many rows of each sheet or CSV the knowledge indexer reads. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_MAX_SHEETS` |  | `20` | How many sheets of a spreadsheet the knowledge indexer reads. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_MAX_UNZIPPED_BYTES` |  | `104857600` | The most a DOCX or XLSX may inflate to. The knowledge indexer refuses one that declares more without opening it, and inflates every entry itself against this budget, whatever the archive declares, before a parser sees it. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FILE_TIMEOUT_MS` |  | `20000` | How long the knowledge indexer lets one file's parser run before stopping its thread and recording the file as timed out. A failed or timed-out file is retried at most twice. | `Modules/Knowledge/ingest/extract/limits.js` |
| `KNOWLEDGE_FLAG_CACHE_TTL_SECONDS` |  | `30` | Seconds a company's knowledgeRetrieval and knowledgeIndexer modes are kept after one read, so a question, a sync and a retry share it; 0 reads the row on every call, which the e2e harness sets so a test sees a switch it just flipped. | `Modules/Knowledge/flag.js` |
| `KNOWLEDGE_INDEXER` |  | `off` | Knowledge indexer: "off" subscribes to nothing and reads or writes no chunk; "tenant" ingests page, comment, call-notes, project-guide and task-attachment events into knowledge_chunks (pages and guides split on headings; attachment text extracted off the request path within the KNOWLEDGE_FILE_* limits; tombstoned on delete, on a deleted task or a removed attachment for comments and files, on project trash for pages, comments, guides and files, and on a member's departure for private pages), backfills each source separately, and lets retrieval read a source's passages from the chunks once that source's backfill is complete, for a company whose knowledgeIndexer.mode is "on"; "all" does so for every company except one set to "off". | `Modules/Knowledge/flag.js` |
| `KNOWLEDGE_RETRIEVAL` |  | `off` | Knowledge retrieval for Ask: "off" gathers sources exactly as before (task fields and page titles by regular expression); "tenant" searches tasks, page bodies and comments through the full-text indexes, and the caller's own call transcripts, with access control applied at query time, for a company whose knowledgeRetrieval.mode is "on" or "hybrid"; "all" does so for every company except one set to "off". A company in "hybrid" also has its chunks embedded as they are indexed (KNOWLEDGE_EMBEDDING_MODEL, with the OpenAI instance key) and each question embedded once, and its answers fuse the lexical and vector candidates by reciprocal rank under the same access filter; "on" stays lexical. | `Modules/Knowledge/flag.js` |
| `KNOWLEDGE_VECTOR_STORE` |  | `local` | Where hybrid knowledge retrieval searches vectors: "local" scores the newest chunks of each source in process from the tenant database (any MongoDB); "atlas", for hosted deployments on MongoDB Atlas, searches the same chunk rows with Atlas Vector Search through one knowledge_chunks_vector index in the database of each tenant whose knowledgeRetrieval mode is "hybrid", created or checked in the background at tenant creation and by the knowledge.backfill job (which also creates it once a tenant switches to hybrid), with the index state shown in the instance knowledge console. Atlas caps the number of search indexes a cluster may hold, lower on shared and free tiers, so check the tier's limit against the number of hybrid tenants. While a tenant's index is being checked, missing, building or the wrong size, or Atlas cannot answer, questions are answered from the lexical side. Any other value is read as "local". | `Modules/Knowledge/vectorStore.js` |
| `LLM_MAX_TOKENS_PLAN` (secret) |  | `32000` | cap for the planning call (raise if you see "ran out of output token budget") | `Modules/AIProjectGenerator/controller.js` |
| `LLM_PRICING` |  |  | JSON map of model name to per-token prices used to report AI spend. | `Modules/AICore/usage.js` |
| `LLM_PROVIDER` |  |  | openai \| anthropic \| deepseek | `Modules/AICore/llmProvider/index.js`, `Modules/Agents/budget.js` |
| `LLM_REGION` |  |  | optional: the inference region the provider is pinned to; shown read-only in the agent settings (GET /api/v2/agents/settings) | `Modules/Agents/budget.js` |
| `LLM_TIMEOUT_MS` |  | `600000` | Model call timeout every provider inherits unless overridden per provider (default 10 min). The agent job lock is model timeout + AGENT_ACT_BUDGET_MS + 2 min. | `Modules/Agents/engine/timeouts.js` |
| `MCP_TOOLS_V2` |  | `off` | MCP tools v2: "on" puts human names and a ref next to the ids in tool results (only names the caller may see), pages every list tool with an opaque cursor (default 25, at most 100 per page), annotates every tool with readOnlyHint, destructiveHint, idempotentHint and openWorldHint from its action rating, and turns a destructive call (an irreversible or workspace-scope write) into a proposal a person approves in the Inbox instead of acting. Default "off" keeps the tool list and result shapes as they were. | `Modules/Mcp/v2Flag.js` |
| `OPENAI_EMBEDDINGS_URL` |  | `https://api.openai.com/v1/embeddings` | The endpoint the OpenAI provider posts embeddings to. Point it at an Azure deployment, a proxy or a self-hosted server that speaks the same API; the e2e harness points it at a stub. Chat completions are unaffected. | `Modules/AICore/llmProvider/openaiProvider.js` |
| `OPENAI_TIMEOUT_MS` |  | `600000` | optional: override the OpenAI axios timeout (default: LLM_TIMEOUT_MS for reasoning/gpt-5, 4 min for others); the agent job lock is derived from the largest provider timeout | `Modules/Agents/engine/timeouts.js` |
| `TENANT_PROVIDER_KEYS` |  |  | Set to on to let each workspace pay for model calls with its own provider keys: owners and admins set one key per provider under Settings, kept in the secrets store by handle with only the handle on the company row, and every model call and embedding run for that workspace is billed to its key, or to the instance key while none is set. A set key that will not open refuses the call rather than billing the instance owner. Needs SECRETS_STORE with SECRETS_KEY; unset or off keeps every call on the instance keys. | `Modules/AICore/providerContext.js` |

## Calling

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `TURN_CREDENTIAL_TTL_SECONDS` (secret) |  | `3600` | Lifetime of TURN credentials minted from TURN_STATIC_AUTH_SECRET. | `Modules/Calls/iceConfig.js` |

## Integrations

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `APIKEY` (secret) |  |  | Firebase web API key used by the server for push notifications. | `Config/config.js`, `Config/contentSecurityPolicy.js` |
| `APPID` |  |  | Firebase app id. | `Config/config.js` |
| `AUTODOMAIN` |  |  | Firebase auth domain. | `Config/config.js` |
| `GITHUB_BASE_OAUTH_URL` |  | `https://github.com/login/oauth` | authorize + access_token base | `Modules/githubOAuth/controller.js` |
| `GITHUB_CLIENT_ID` |  |  | GitHub OAuth app client id. | `Modules/githubOAuth/controller.js` |
| `GITHUB_CLIENT_SECRET` (secret) |  |  | GitHub OAuth app client secret. | `Modules/githubOAuth/controller.js` |
| `GITHUB_OAUTH_REQUIRED` |  | `true` | "true" requires server-side verification via GitHub /user (recommended) | `Modules/Auth/controller/authHelpers.js` |
| `GITHUB_TOKEN` (secret) |  |  | Token used to read release notes from GitHub for the changelog page. | `Modules/Changelog/controller.js` |
| `GITLAB_BASE_API_URL` |  | `https://gitlab.com/api/v4` | used to verify the identity (/user) | `Modules/Auth/controller/authHelpers.js` |
| `GITLAB_BASE_OAUTH_URL` |  | `https://gitlab.com/oauth` | authorize + token base | `Modules/gitlabOAuth/controller.js` |
| `GITLAB_CLIENT_ID` |  |  | GitLab OAuth app client id. | `Modules/gitlabOAuth/controller.js` |
| `GITLAB_CLIENT_SECRET` (secret) |  |  | GitLab OAuth app client secret. | `Modules/gitlabOAuth/controller.js` |
| `GITLAB_OAUTH_REQUIRED` |  | `true` | "true" requires server-side verification via GitLab /user (recommended) | `Modules/Auth/controller/authHelpers.js` |
| `GOOGLE_CLIENT_ID` |  |  | used for token exchange AND id-token verification | `Modules/googleOAuth/controller.js` |
| `GOOGLE_CLIENT_SECRET` (secret) |  |  | Google OAuth client secret. | `Modules/googleOAuth/controller.js` |
| `GOOGLE_OAUTH_CLIENT_ID` |  |  | Google OAuth client id accepted when verifying Google sign-in tokens. | `Modules/Auth/controller/authHelpers.js` |
| `GOOGLE_OAUTH_REQUIRED` |  | `true` | "true" rejects logins that fail id-token verification (recommended) | `Modules/Auth/controller/authHelpers.js` |
| `GOOGLE_OAUTH_URL` |  | `https://oauth2.googleapis.com/token` | Google token endpoint | `Modules/googleOAuth/controller.js` |
| `MEASUREMENTID` |  |  | Firebase analytics measurement id. | `Config/config.js` |
| `MESSAGINGSENDERID` |  |  | Firebase messaging sender id. | `Config/config.js` |
| `PROJECTID` |  |  | Firebase project id. | `Config/config.js` |
| `SERVICE_FILE` |  | `./firebase-adminsdk.json` | Path to Firebase service account JSON (must be inside project root) | `Config/config.js`, `utils/safeServiceFile.js` |
| `STORAGEBUCKET` |  |  | Firebase storage bucket. | `Config/config.js` |

## Billing

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `FREE_COMPANY_COUNT` |  |  | Companies a user may create on the free plan. | `Modules/Company/controller.js` |
| `PAYMENTMETHOD` |  |  | Payment provider used at sign-up (chargebee or paddle). | `Modules/Company/controller.js` |

## Security

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AGENT_EGRESS_ALLOWLIST` |  | `false` | Set to true to route every agent fetch through the workspace egress gateway: a workspace whose list under Instance > Egress names hosts lets its agents fetch those hosts only, checked on every redirect hop and again against the resolved address, with each refusal audited as agent.egress_refused naming the host. Entries are exact hostnames or *.suffix patterns with an optional port; an entry without a port allows that host on any port and over plain http as well as https, so write host:443 to allow https on its default port only. Wildcards on shared-hosting and wildcard-DNS suffixes (*.github.io, *.nip.io, ...) are refused. A workspace with no list, and every workspace while this is unset or false, fetches as before: public hosts allowed, private, metadata and link-local hosts refused. Private hosts stay refused even when listed. Turning this on also judges NAT64, 6to4 and IPv4-compatible IPv6 addresses by the IPv4 address they carry, and that rule is shared, so it applies to webhook URL checks on save and to the Integrations public-URL check as well. Not behind the gateway: model provider and embeddings calls, SMTP, file storage, and webhooks fired by task changes an agent makes. | `Modules/Agents/engine/egressContext.js` |
| `AGENT_TAINT_ROUTING` |  | `off` | Set to on (or true) to mark an agent run as tainted once its context takes in content from outside the workspace's own members (a fetched page, an email-in body, a form submission, a webhook payload, uploaded file text or a retrieved passage whose origin is external), and to route a tainted run's risky writes (any escalation under the risk rating, or a write outside the run's own project) to approval with a reason that names the taint. The run, its replay rows, its audit rows and its proposals record where the content came from (a host, an id or a hash), never the content. Unset or off leaves runs, decisions and records exactly as they were. | `Modules/Agents/taint.js` |
| `API_TOKEN_MAX_DAYS` |  | `365` | Longest lifetime, in days, of an API token while API_TOKEN_STRICT is on. Can only lower the 365-day maximum: a value above 365 or not a whole number of days is ignored with a warning at boot. Ignored while API_TOKEN_STRICT is off. | `Modules/ApiTokens/helpers/apiTokenRules.js` |
| `API_TOKEN_STRICT` |  | `false` | Set to true to require an expiry and at least one scope on every new API token, and to allow a token without an expiry only to be revoked. A token without an expiry keeps working for 30 days from the later of its creation and when this was first on for the instance (recorded once as apiTokenStrictSince on the instance settings document and kept across backup restores), then stops. A new token may last at most API_TOKEN_MAX_DAYS (365 by default); an older token that expires later works for that many days from the later of its creation and when the maximum first applied (recorded once as apiTokenMaxLifetimeSince, also kept across restores), then stops. Unset or false keeps the old rules. | `Modules/ApiTokens/helpers/apiTokenRules.js` |
| `AUDIT_CHAIN` |  | `false` | Set to true to chain every new audit row per company: each row carries a sequence number, the previous row's hash and an HMAC-SHA256 keyed with AUDIT_CHAIN_KEY, a change to a row is appended as a new row instead of editing it, retention anchors the rows it deletes and keeps audit rows at least as long as agent runs, and the audit log reports each row as verified, broken or unchained. Names, email, IP address and user agent are outside the hash. Needs AUDIT_CHAIN_KEY; without a valid key the chain stays off and an error is logged. Unset or false keeps rows unchained and edited in place. | `Modules/Audit/helpers/chainRules.js` |
| `AUDIT_CHAIN_KEY` (secret) |  |  | Secret of at least 32 characters that keys the audit chain hash, so someone who can write to the database cannot recompute it. Rows chained under one key only verify under that key; keep it out of the database and its backups. | `Modules/Audit/helpers/chainRules.js` |
| `AUDIT_EXPORT_MAX_ROWS` |  | `100000` | Most rows one audit-log CSV export streams; a last row says when the filter held more. Can only lower the hard cap. | `Modules/Audit/controller.js` |
| `AUDIT_RETENTION_DAYS` |  |  | Days to keep audit-log entries before the recorder prunes them. | `Modules/Audit/recorder.js` |
| `AUTH_RATE_LIMIT_BLOCK_MS` |  | `1800000` | How long an IP stays blocked after too many failed logins. | `Modules/Auth/helper.js` |
| `AUTH_RATE_LIMIT_MAX_ATTEMPTS` |  | `5` | Failed login attempts allowed per window before blocking. | `Modules/Auth/helper.js` |
| `AUTH_RATE_LIMIT_WINDOW_MS` |  | `900000` | Window over which failed login attempts are counted. | `Modules/Auth/helper.js` |
| `CORS_ORIGINS` |  | `http://localhost:8080` | Extra origins allowed by CORS, comma-separated (WEBURL and APIURL are always allowed). | `utils/cors.js` |
| `CSP_EXTRA_CONNECT_SRC` |  |  | Extra hosts for the connect-src directive of the content security policy (API, XHR and websocket calls), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_FONT_SRC` |  |  | Extra hosts for the font-src directive of the content security policy (fonts), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_FORM_ACTION` |  |  | Extra hosts for the form-action directive of the content security policy (form posts), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_FRAME_ANCESTORS` |  |  | Extra hosts for the frame-ancestors directive of the content security policy (sites that may frame AlianHub), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_FRAME_SRC` |  |  | Extra hosts for the frame-src directive of the content security policy (framed pages), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_IMG_SRC` |  |  | Extra hosts for the img-src directive of the content security policy (images), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_MEDIA_SRC` |  |  | Extra hosts for the media-src directive of the content security policy (audio and video), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_SCRIPT_SRC` |  |  | Extra hosts for the script-src directive of the content security policy (scripts), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. In script-src and worker-src, sources must be https, may not be on a domain where anyone can publish (github.io, googleusercontent.com, githubusercontent.com and similar) must name exact hosts (no wildcard), and on a package CDN (cdn.jsdelivr.net, unpkg.com, cdnjs.cloudflare.com) must name one package and its version. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_STYLE_SRC` |  |  | Extra hosts for the style-src directive of the content security policy (style sheets), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_EXTRA_WORKER_SRC` |  |  | Extra hosts for the worker-src directive of the content security policy (workers), separated by spaces and written with their scheme, such as https://cdn.example.com or https://*.example.com. Quotes, keywords such as 'unsafe-eval', semicolons, commas, schemes on their own, a bare * and a wildcard over a top-level domain, a public suffix or a shared hosting domain stop the server at boot, whatever CSP_MODE is. In script-src and worker-src, sources must be https, may not be on a domain where anyone can publish (github.io, googleusercontent.com, githubusercontent.com and similar) must name exact hosts (no wildcard), and on a package CDN (cdn.jsdelivr.net, unpkg.com, cdnjs.cloudflare.com) must name one package and its version. Read at boot. | `Config/contentSecurityPolicy.js` |
| `CSP_MODE` |  | `off` | Content security policy: off (no policy header, the default), report (Content-Security-Policy-Report-Only: nothing is blocked, and what would be is counted under Instance > Enforcement) or enforce (Content-Security-Policy: the browser blocks it). The policy is built on the first request from the storage endpoint and whether push is configured (restart to change them), and follows WEBURL and APIURL when they change; the public share and form pages keep their own stricter policy. Violation reports go to POST /api/v2/csp-report, which keeps the day, directive, blocked host and page path shape for 30 days and nothing else. Run report until the list is quiet, then enforce. An unknown value stops the server at boot. Read at boot. | `Config/contentSecurityPolicy.js` |
| `DISABLE_PERMISSION_ENFORCEMENT` |  |  | Emergency switch, never for normal running. Set to true to skip the per-key permission checks for API tokens and project edits, and to judge a workspace set to enforce in report mode: recorded, never refused. That covers browser sessions and API tokens on the task actions mapped after the first nine (Config/taskWritePermissions.js). Role guards stay on for API tokens, and so does the refusal of a task write body that names another company or an id that cannot be read. | `Config/permissionEnforcement.js`, `Config/permissionGuard.js` |
| `ENDPOINT_KEY` (secret) |  |  | Shared key expected by the internal service endpoints. | `Config/config.js` |
| `GLOBAL_RATE_LIMIT_PER_MIN` |  |  | Requests per minute allowed per IP by the global rate limiter. | `index.js` |
| `HELMET_ENABLED` |  |  | Send the helmet security headers (default true). | `Config/securityHeaders.js` |
| `INSTANCE_ADMIN_KEY` (secret) |  |  | Shared key that grants the instance-admin API without a product-owner session. | `Modules/Instance/guard.js` |
| `MCP_CURSOR_SECRET` |  |  | Key that signs the MCP list cursors (HMAC-SHA256) while MCP_TOOLS_V2 is on. A cursor is bound to the company, the caller, the token and the query, and expires after an hour. Unset derives a key from JWT_SECRET; changing it ends the cursors in flight. | `Modules/Mcp/cursor.js` |
| `MCP_OAUTH` |  | `off` | Set to on to run the OAuth 2.1 authorization server for the MCP endpoint (MCP authorization spec 2025-11-25): GET /.well-known/oauth-authorization-server, /oauth/authorize (authorization code with PKCE S256 only, resource required and equal to <issuer>/mcp, exact redirect URI match except that an http loopback redirect may use any port, https or loopback redirects; scope may be left out for read-only access), /oauth/token (single-use codes, rotating refresh tokens, a reused code or refresh token revokes its whole grant), /oauth/revoke (RFC 7009), and /api/v2/oauth-clients for workspace owners and admins to pre-register clients. Clients may also identify themselves with a client ID metadata document, fetched through the agents' SSRF-safe fetch (5 KB, 5 s, no redirects, one fetch per URL at a time, a failed URL retried after 5 s). Codes, tokens and client secrets are stored only as keyed hashes; every token is bound to one workspace, one person and the /mcp resource. The MCP endpoint does not accept these tokens yet, and the consent screen is not built yet, so an authorization cannot complete outside a test process. Unset or off registers none of these routes. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_ACCESS_TOKEN_MINUTES` |  | `15` | Lifetime of an MCP OAuth access token, in minutes. Never longer than the grant it belongs to. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_CLIENT_METADATA_CACHE_SECONDS` |  | `300` | How long a fetched client ID metadata document is cached when it sends no Cache-Control max-age (a max-age is followed up to a day; no-store is never cached). | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_DCR` |  | `off` | With MCP_OAUTH on, set to on to open RFC 7591 dynamic client registration at /oauth/register and advertise it in the server metadata. Off by default: clients use a client ID metadata document or are pre-registered by a workspace owner or admin. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_GRANT_MAX_DAYS` |  | `90` | Longest an MCP OAuth grant lives, in days, however often its refresh token is rotated; after it the person authorizes the client again. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_ISSUER` |  |  | Issuer URL of the MCP authorization server, and the base of its endpoints and of the /mcp resource tokens are bound to. Defaults to APIURL. It must be an origin (no path, query or fragment) and https; plain http is accepted only on localhost, 127.0.0.1 or [::1] while NODE_ENV is unset, development or test. With MCP_OAUTH on, any other value stops the server at boot. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_RATE_LIMIT_PER_MIN` |  | `30` | Requests per minute per client address to /oauth/authorize, and separately to /oauth/token with /oauth/revoke, before the MCP authorization server answers 429. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_REFRESH_TOKEN_DAYS` |  | `30` | Lifetime of an MCP OAuth refresh token, in days. Each use spends it and issues a new one; never longer than the grant it belongs to. | `Modules/OAuthServer/config.js` |
| `MCP_OAUTH_TOKEN_SECRET` (secret) |  |  | Key for the keyed hashes under which MCP OAuth codes, tokens and client secrets are stored, separate from JWT_SECRET, which is used when this is unset. Changing it invalidates every issued token and every pre-registered client secret. | `Modules/OAuthServer/tokenHash.js` |
| `MEMBERSHIP_CACHE_TTL_SECONDS` |  | `60` | Seconds a user-company membership check is cached before MongoDB is asked again. | `Config/jwt.js` |
| `PERMISSION_ENFORCEMENT_CACHE_TTL_SECONDS` |  | `30` | Seconds a workspace's own permissionEnforcement.mode is cached before the company row is read again; 0 reads it on every guarded request. A failed read inherits the instance default for 5 seconds whatever this is set to. | `Config/permissionEnforcement.js` |
| `PERMISSION_ENFORCEMENT_MODE` |  | `off` | Server-side permission checks for browser sessions: off (they pass as before), report (they pass, and each would-be denial is recorded in permission_decisions) or enforce (they are refused, and a check that cannot complete fails closed). The default for every workspace; a company row's permissionEnforcement.mode overrides it. Anything unrecognised reads as off. API tokens are always enforced on the keys they were refused on before, including nine task actions; on every other task write in Config/taskWritePermissions.js they follow this mode like browser sessions. A task write body that names another company or an id that cannot be read is refused for API tokens in every mode, and follows this mode for browser sessions. Also editable under Instance > Settings > Security. | `Config/permissionEnforcement.js` |
| `PRECOMPANYKEY` (secret) | yes | `your_preset_key` | Random key for the preset company routes, presented in the x-preset-key header; generated at setup. | `Config/config.js` |
| `SECRETS_KEY` (secret) |  |  | Key of at least 32 characters that seals the secrets store, separate from JWT_SECRET. Each stored secret records the id of the key that sealed it (derived from the stretched key, so it is no shortcut for guessing the key); keep the key out of the database and its backups. To rotate, move the old value to SECRETS_KEY_PREVIOUS, set the new one here, restart, and run `npm run secrets:reencrypt`. | `Config/secrets.js` |
| `SECRETS_KEY_PREVIOUS` (secret) |  |  | The previous SECRETS_KEY during a key rotation: secrets sealed under it still resolve, and `npm run secrets:reencrypt` reseals every company under SECRETS_KEY, writes an audit row per company and prints how many live secrets are still on this key. Unset it only when that command says it is safe to. | `Config/secrets.js` |
| `SECRETS_STORE` |  | `false` | Set to true to keep integration and webhook secrets in a per-company secrets store, referenced by handle: a new or updated secret is sealed with AES-256-GCM under SECRETS_KEY and bound to its company and handle, the integration or webhook document keeps only the handle, every create, rotation, revocation and failed lookup is audited, and owners and admins see each secret's name, kind, key id and dates under Settings → Integrations, where they can rotate or revoke it. Migration 031 moves existing secrets in when this is on while it runs. Needs SECRETS_KEY; with a missing or short key the store refuses every read and write, connecting an integration or adding a webhook is refused rather than saved outside the store, and an error is logged. Unset or false keeps secrets sealed on their documents as before; a handle already written still resolves while a key is set, and a secret saved while this is off replaces the handle on its document, so the newest value is always the one in use. Turning this off again: the old store copy is revoked when SECRETS_KEY is still set, and otherwise marked, then revoked by `npm run secrets:reencrypt` (or by migration 031 when it runs again) once a key is set. | `Config/secrets.js` |
| `SKILL_EXTERNAL_READS` |  | `off` | Declared external reads in data skills: "on" offers the url and api readers in the Skill Library, each naming one exact host, a path template, GET only, at most 512 KB, 10 s and 3 redirects, and optionally the handle of a skill_read workspace secret. A skill is saved only when every declared host is on the workspace egress allowlist (an empty list refuses them all, and private or reserved hosts are refused even when listed), and a raw secret anywhere in the skill body is refused. Running such a skill fails with external_reads_not_available until the run-time slice lands; nothing is fetched. Default "off" hides the readers, refuses them on save and leaves every skill as it was. | `Modules/Agents/skills/externalReads.js` |
| `SOCKETIO_ADMIN_PASSWORD_HASH` (secret) |  | `$2a$12$HHemhzmTrC8Dmf2Gd9v/Teo9oiCxfYJb.St3HKMBx1L3wJmZLeX5u` | bcrypt hash of the Socket.IO admin UI password; the UI stays off while unset. | `socket/socketinit.js` |
| `SOCKETIO_ADMIN_USERNAME` |  | `alian` | Username for the Socket.IO admin UI; the UI stays off while unset. | `socket/socketinit.js` |
| `STEP_CREDENTIALS` |  | `off` | Set to on to give the platform its own service identities and mint a step-scoped credential per workflow step: the workflow engine, its worker, the knowledge indexer and the model router record their rows as service:<name> instead of a borrowed user, and when the engine claims a step it signs a short-lived credential carrying the company, run, step, fencing token, actor context and allowed actions that expires one lease after it was minted, is re-minted whenever a heartbeat extends the lease, and is checked against the live step row whenever an agent step's actor presents it to perform an action. The tokens screen lists live credentials as step-scoped. Unset or off keeps today's behaviour: no credential is minted and rows are attributed as before. | `Modules/Agents/serviceIdentity.js` |
| `STEP_CREDENTIAL_SECRET` (secret) |  |  | Key material for signing step-scoped credentials (STEP_CREDENTIALS). Derived with scrypt, never used as-is; falls back to JWT_SECRET, so set a dedicated secret in production so rotating JWT_SECRET does not invalidate credentials of steps in flight, and keep it out of the database and its backups. | `Modules/Workflows/stepCredential.js` |
| `WEBHOOK_ALLOWED_PRIVATE_HOSTS` |  |  | Private hosts webhooks may post to: exact hostnames or CIDR ranges, comma-separated (empty by default, so every private host is refused). Also editable under Instance > Settings > Security. Link-local and cloud metadata addresses stay blocked, and a range can be no wider than /8 (IPv4) or /32 (IPv6). | `Modules/Webhooks/helpers/privateHostAllowlist.js` |

## Logging

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `FATAL_FLUSH_TIMEOUT_MS` |  | `3000` | Upper bound, in milliseconds, that a fatal crash waits for flushers (log files, telemetry exporter, crash report mail) before the process exits with 1. | `Config/processGuards.js` |
| `LOG_DATE_PATTERN` |  | `YYYY-MM-DD` | Date pattern appended to rotated log file names. | `Config/loggerConfig.js` |
| `LOG_DIR` |  | `log` | Directory that receives the rotated log files. | `Config/loggerConfig.js`, `Modules/Instance/logsPath.js` |
| `LOG_FORMAT` |  | `text` | Log file record format: text (one formatted line) or json (one { ts, level, label, requestId, traceId, message } record per line). | `Config/logFormat.js`, `Config/loggerConfig.js` |
| `LOG_MAX_FILES` |  | `14d` | Retention window for rotated log files (winston-daily-rotate-file). | `Config/loggerConfig.js` |
| `LOG_MAX_SIZE` |  | `20m` | Size at which a log file rotates. | `Config/loggerConfig.js` |
| `OTEL_EXPORTER_OTLP_ENDPOINT` |  |  | OTLP/HTTP collector base URL (traces go to <endpoint>/v1/traces). Unset keeps the OpenTelemetry SDK and exporter off; trace ids are still recorded. | `Config/telemetry.js` |
| `OTEL_SERVICE_NAME` |  | `alianhub` | service.name reported on exported spans. | `Config/telemetry.js` |

## Limits

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `BODY_LIMIT` |  | `2mb` | Maximum JSON/urlencoded/raw request body (body-parser size string). | `index.js` |
| `CLOUD_STORAGE_MAX_IMPORT_BYTES` |  |  | Largest file the cloud-storage importer will copy into a project. | `Modules/CloudStorage/controller.js` |
| `FORM_MAX_FILES` |  | `3` | Maximum attachments on one public form submission. | `Modules/Forms/helpers/formUpload.js` |
| `FORM_MAX_FILE_BYTES` |  |  | Maximum size of one public form attachment. | `Modules/Forms/helpers/formUpload.js` |
| `MAX_IMAGE_FILE_BYTES` |  | `26214400` | Largest image accepted for processing. | `utils/imageGuard.js` |
| `MAX_IMAGE_PIXELS` |  | `50000000` | Largest image, in pixels, accepted for processing (decompression-bomb guard). | `utils/imageGuard.js` |
| `UPLOAD_MAX_FILES` |  | `20` | Maximum files per upload request. | `utils/uploadConfig.js` |
| `UPLOAD_MAX_FILE_BYTES` |  |  | Maximum size of one uploaded file. | `utils/uploadConfig.js` |

## Jobs

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AGENT_ALERTS_INTERVAL_MINUTES` |  | `15` | Minutes between runs of the agent rate-alert evaluator (error rate, approval rate, cost forecast, queue age). | `Modules/Agents/alerts.js` |
| `AGENT_PROPOSAL_STUCK_MINUTES` |  | `10` | Minutes an agent proposal may sit in 'applying' before the reaper fails it. | `Modules/Agents/proposals.js` |
| `AUTOMATION_CONCURRENCY` |  | `5` | Automation runs processed in parallel. | `Modules/Automations/engine/index.js` |
| `AUTOMATION_ENGINE` |  | `true` | Set to false to stop the automation engine from starting. | `Modules/Automations/engine/index.js` |
| `AUTOMATION_EVENT_LOG` |  |  | Set to true to record every domain event in automation_event_log for debugging automations. | `event/domainEventBus.js` |
| `AUTOMATION_QUEUE_DRIVER` |  | `agenda` | Queue driver for automation runs (agenda or memory). | `Modules/Automations/engine/index.js`, `Modules/Instance/controller.js` |
| `CRON_TZ` |  | `UTC` | IANA time zone for scheduled jobs (reminders, digests). | `Modules/GeneralReminders/scheduler.js`, `Modules/Instance/controller.js` +1 |
| `GENERAL_REMINDERS_CRON` |  |  | Cron expression for the general reminders scheduler. | `Modules/GeneralReminders/scheduler.js` |
| `NOTIFICATIONACTIVETIME` |  |  | Minutes a user counts as active for notification batching. | `Modules/notification/prepare-notification-data/user-controllerV2.js` |
| `WORKFLOW_APPROVAL_DEADLINE_MS` |  | `259200000` | Deadline given to an approval step whose definition names none, so a run cannot wait forever on a decision nobody will make. | `Modules/Workflows/flag.js` |
| `WORKFLOW_APPROVAL_POLL_MS` |  | `300000` | How often a workflow step waiting on a human approval is looked at again. A decision wakes the step immediately; this is the floor that notices a deadline nobody answered. | `Modules/Workflows/flag.js` |
| `WORKFLOW_BACKOFF_MS` |  | `30000,120000,600000` | Comma-separated backoff ladder, in milliseconds, a transient workflow step failure waits between attempts. | `Modules/Workflows/flag.js` |
| `WORKFLOW_ENGINE` |  | `off` | Durable workflow runs for automation rules: "off" runs a rule through the automation runner exactly as before, "on" makes it a one-node workflow run with a lease, a claim and engine-scheduled retries. | `Modules/Workflows/flag.js` |
| `WORKFLOW_HEARTBEAT_MS` |  |  | How often a running workflow step renews its lease. Defaults to a third of WORKFLOW_LEASE_MS. | `Modules/Workflows/flag.js` |
| `WORKFLOW_JOIN_POLL_MS` |  | `2000` | How often a fan-in join or a loop checks whether the steps it is waiting on have finished. | `Modules/Workflows/flag.js` |
| `WORKFLOW_LEASE_MS` |  |  | How long a claimed workflow step run keeps its lease before another worker may reclaim it. Defaults to the agent run lock, so the two cannot drift. | `Modules/Workflows/flag.js` |
| `WORKFLOW_MAX_ATTEMPTS` |  | `3` | Attempts a workflow step gets before a transient failure is given up on. | `Modules/Workflows/flag.js` |
| `WORKFLOW_MAX_FAN_OUT` |  | `50` | Children one fan-out step may expand into. A workflow definition may ask for fewer, never more; over the bound the step fails instead of doing part of the work. | `Modules/Workflows/flag.js` |
| `WORKFLOW_MAX_LOOP_ITERATIONS` |  | `25` | Iterations a loop step may re-enter its body for. The ceiling a definition's own maxIterations is capped to. | `Modules/Workflows/flag.js` |
| `WORKFLOW_MAX_RUNS_PER_HOUR` |  | `0` | Agent runs an hour, per company and per agent, that a workflow loop may admit an iteration for. The ceiling a loop's own maxRunsPerHour and a rule's stored limits.maxRunsPerHour are capped to; 0 means no ceiling for the installation. | `Modules/Workflows/flag.js` |
| `WORKFLOW_RUN_BUDGET_USD` |  |  | Ceiling on what a whole workflow run may spend, in dollars. Each step's cost is taken off the run's remaining budget; a step asking for more than remains is refused and the run is marked blocked. Unset means no ceiling. | `Modules/Workflows/flag.js` |
| `WORKFLOW_RUN_DEADLINE_MS` |  |  | Ceiling on how long a whole workflow run may take, in milliseconds. Each step takes its share and hands on what is left; a step asking for more than remains is refused and the run is marked blocked. Unset means no ceiling. | `Modules/Workflows/flag.js` |
| `WORKFLOW_RUN_LIMIT_CACHE_MS` |  | `60000` | How long an hour of an agent's run start times is trusted before it is read again. The hour rolls in memory between reads; this is only how quickly a run another process started is noticed. | `Modules/Workflows/flag.js` |
| `WORKFLOW_TENANT_CONCURRENCY` |  | `5` | Workflow step runs one tenant may have claimed at once, counted across every worker. | `Modules/Workflows/flag.js` |

## Frontend

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `VUE_APP_AFFILIATEON` |  |  | Show the affiliate programme entry points. | `frontend/src/App.vue`, `frontend/src/config/env.js` |
| `VUE_APP_APIKEY` (secret) |  |  | Firebase web API key (push notifications). | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_APPID` |  |  | Firebase app id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_AUTHDOMAIN` |  |  | Firebase auth domain. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_CUSTOMFIELDID` |  |  | Custom field id used by the support product link. | `frontend/src/components/organisms/TaskDetailOverlay/TaskDetailPanel.vue` |
| `VUE_APP_CUSTOMFIELDPRODUCTID` |  |  | Custom field id that stores the product on support tickets. | `frontend/src/components/organisms/TaskDetailOverlay/TaskDetailPanel.vue` |
| `VUE_APP_GITHUB_BASE_OAUTH_URL` |  |  | GitHub OAuth authorize URL. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GITHUB_CLIENT_ID` |  |  | GitHub OAuth client id for the login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GITLAB_BASE_OAUTH_URL` |  |  | GitLab OAuth authorize URL. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GITLAB_CLIENT_ID` |  |  | GitLab OAuth client id for the login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GOOGLE_CLIENT_ID` |  |  | Google OAuth client id for the login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_GITHUB_LOGIN` |  |  | Show the GitHub login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_GITLAB_LOGIN` |  |  | Show the GitLab login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_GOOGLE_LOGIN` |  |  | Show the Google login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_SSO_LOGIN` |  |  | Show the SSO login entry point. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_MEASUREMENTID` |  |  | Firebase analytics measurement id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_MESSAGINGSENDERID` |  |  | Firebase messaging sender id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_PROJECTID` |  |  | Firebase project id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_STORAGEBUCKET` |  |  | Firebase storage bucket. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_STORAGE_TYPE` |  |  | Storage backend the web app assumes (server or wasabi); mirror STORAGE_TYPE. | `frontend/src/config/env.js`, `frontend/src/config/publicConfig.js` |
| `VUE_APP_SUPPORT_MAIL` |  |  | Mailbox shown for support requests. | `frontend/src/views/Projects/Comments/Comments.vue` |
| `VUE_APP_SUPPORT_PROJECTID` |  |  | Project id that receives support tickets raised from the app. | `frontend/src/components/organisms/TaskDetailOverlay/TaskDetailPanel.vue` |
| `VUE_APP_USEREMAIL` |  |  | Default account email pre-filled on demo builds. | `frontend/src/views/Projects/Comments/Comments.vue` |

## General

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `ALIANHUB_DOCKER` |  |  | Set by the Docker image so defaults suit a container (storage under /app/storage). | `Modules/Instance/controller.js` |
| `BACKUP_DIR` |  | `backups` | Directory where in-app backups are written (default backups/). | `Modules/Instance/backups.js` |
| `CRON_ENABLED` |  |  | Run the scheduled jobs in this process (default true; set false on extra replicas). | `Modules/Instance/controller.js`, `index.js` |
| `DEMO_EMAIL` |  |  | Email of the demo account shown on the login page when DEMO_MODE is on. | `Modules/Admin/common/controller.js` |
| `DEMO_MODE` |  | `false` | Set to true to show the demo login banner and credentials. | `Modules/Admin/common/controller.js` |
| `DEMO_PASSWORD` (secret) |  |  | Password of the demo account shown when DEMO_MODE is on. | `Modules/Admin/common/controller.js` |
| `ERRORRECIVEREMAIL` |  | `admin@yourdomain.com` | Email to receive error alerts | `Config/config.js` |
| `HEALTH_DB_TIMEOUT_MS` |  |  | How long /health waits for the database ping before reporting it down (default 2000). | `Modules/Instance/health.js` |
| `MIGRATIONS_AUTO` |  |  | Apply pending migrations at boot (default true; false only reports them). | `Modules/Instance/controller.js` |
| `MONGODB_URL` | yes |  | mongo connection string (no trailing slash) | `Config/config.js`, `Modules/AICore/persistence.js` +9 |
| `NODEMAILER_EMAIL_PASSWORD` (secret) |  |  | Use app password for Gmail | `Config/config.js` |
| `NODEMAILER_PORT` |  | `587` | 587 (STARTTLS) or 465 (SSL) | `Config/config.js` |
| `NOOFPRESETCOMPANY` |  | `10` | Number of preset companies seeded at first run. | `Config/config.js` |
| `RESEND_API_KEY` (secret) |  |  | Get from resend.com | `Config/config.js`, `Modules/Instance/controller.js` |
| `RESEND_FROM_EMAIL` |  | `onboarding@resend.dev` | Use verified domain in production | `Config/config.js` |
| `TRANSLATE_API_KEY` (secret) |  |  | Machine-translation key used by scripts/i18n-backfill.js; unset means English copies are written for review. | `scripts/i18n-backfill.js` |
