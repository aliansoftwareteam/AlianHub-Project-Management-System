# Environment variables

Generated by `node scripts/env-doc.js` from the variables the code reads; `node scripts/env-doc.js --check` fails when this file, `.env.example` or `frontend/.env.example` drifts. Descriptions live in `scripts/env-doc.meta.json`.

Backend variables: 151. Frontend build-time variables: 23.

## Server

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `APIURL` | yes |  | Public base URL of this API, with a trailing slash (used in emails, OAuth callbacks and share links). | `Config/config.js`, `Modules/ApiTokens/controller.js` +6 |
| `APP_NAME` |  | `Alian Hub` | Product name shown in emails and page titles. | `Config/config.js`, `Modules/Template/forgotPassword.js` +5 |
| `BUILD_INFO_GIT_TIMEOUT_MS` |  | `10000` | Per-command timeout, in ms, for the git calls that resolve the running version. On timeout the server reports channel unknown and retries in the background. | `scripts/build-info.js` |
| `NODE_ENV` |  | `development` | development \| production | `Config/config.js`, `Modules/AICore/persistence.js` +5 |
| `PORT` | yes | `4000` | TCP port the HTTP server listens on. | `Config/config.js` |
| `SERVER_HEADERS_TIMEOUT_MS` |  | `66000` | How long the HTTP server waits for a full request header block; always kept above the keep-alive timeout (default 66000). | `Modules/Agents/engine/timeouts.js` |
| `SERVER_KEEP_ALIVE_TIMEOUT_MS` |  | `65000` | Idle keep-alive timeout of the HTTP server; keep it above the idle timeout of any proxy in front (default 65000). | `Modules/Agents/engine/timeouts.js` |
| `SERVER_REQUEST_TIMEOUT_MS` |  | `300000` | How long the HTTP server waits to receive a whole request (default 300000). Server-sent event streams are exempt. | `Modules/Agents/engine/timeouts.js` |
| `TRUST_PROXY` |  | `loopback` | Express trust proxy setting; loopback by default so X-Forwarded-For is honoured behind a local reverse proxy. | `index.js` |
| `UNDER_MAINTENANCE` |  | `false` | true \| false | `Config/config.js` |
| `WEBURL` |  | `http://localhost:4000` | Public base URL of the web app, used in emails and CORS. | `Config/config.js`, `Modules/Forms/controller.js` +2 |

## Database

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `MONGO_CONNECT_TIMEOUT_MS` |  | `60000` | How long a new MongoDB connection may take to establish (default 60000). | `Modules/Agents/engine/timeouts.js` |
| `MONGO_MIN_POOL_SIZE` |  |  | Minimum connections kept open per company database. | `utils/mongo-handler/mongoConnector.js` |
| `MONGO_POOL_SIZE` |  |  | Maximum connections per company database. | `utils/mongo-handler/mongoConnector.js` |
| `MONGO_SERVER_SELECTION_TIMEOUT_MS` |  | `60000` | How long the MongoDB driver waits to find a usable server before failing an operation (default 60000). | `Modules/Agents/engine/timeouts.js` |
| `MONGO_SOCKET_TIMEOUT_MS` |  | `300000` | Inactivity timeout on a MongoDB socket before it is closed (default 300000). | `Modules/Agents/engine/timeouts.js` |
| `MONGO_WAIT_QUEUE_TIMEOUT_MS` |  |  | How long a query waits for a free connection before failing. | `utils/mongo-handler/mongoConnector.js` |

## Auth

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `JWT_ALGORITHM` |  | `HS256` | Algorithm used to sign JWTs. | `Config/jwt.js`, `Modules/Auth/helpers/refreshTokenRules.js` +1 |
| `JWT_EXP` |  | `24h` | Access token lifetime (jsonwebtoken duration string). | `Config/jwt.js`, `Modules/Auth/controller/loginSession.js` +3 |
| `JWT_SECRET` (secret) | yes | `your_jwt_secret_here` | Secret used to sign JWTs and derive encryption keys. | `Config/instanceSettings.js`, `Config/jwt.js` +5 |
| `MAGIC_LINK_ENABLED` |  | `true` | Set to false to disable passwordless magic-link sign-in. | `Modules/Auth/controller/magicLink.js` |
| `REFRESH_TOKEN_REUSE_GRACE_SECONDS` |  | `10` | Seconds after a refresh token is rotated during which presenting the old one again is refused without revoking the session, so parallel requests or tabs do not log the user out. Past this window a reused token revokes the session. | `Modules/Auth/helpers/refreshTokenRules.js` |
| `SESSIONEXPIREDTIME` |  | `172800` | Idle time after which a login session expires. | `Modules/Auth/controller/loginSession.js`, `Modules/Auth/helpers/refreshTokenRules.js` +3 |
| `TRACKER_PKCE_LEGACY_UNTIL` |  |  | ISO date from which desktop tracker builds that sign in without a PKCE verifier are refused. Unset (the default) keeps those builds working; an unparseable value reads as unset. | `Modules/Auth/helpers/trackerCode.js` |
| `TWO_FACTOR_ENC_KEY` (secret) |  |  | key material for encrypting TOTP secrets at rest | `Modules/Auth/helpers/twoFactorRules.js` |
| `TWO_FACTOR_ISSUER` |  | `AlianHub` | label shown in the user's authenticator app | `Modules/Auth/helpers/twoFactorRules.js` |
| `TWO_FACTOR_TEMP_SECRET` (secret) |  | `${process.env.JWT_SECRET || ''}::2fa-temp` | signs the short-lived login second-step (temp) token | `Modules/Auth/helpers/twoFactorRules.js` |

## Mail

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AWS_SES_FROM_DEFAULT` |  |  | Default From address when mail is sent through AWS SES. | `Config/config.js` |
| `AWS_SES_KEY` (secret) |  |  | AWS access key for SES mail delivery. | `Config/aws.js`, `Config/config.js` |
| `AWS_SES_REGION` |  |  | AWS region of the SES account. | `Config/aws.js`, `Config/config.js` |
| `AWS_SES_SECRET` (secret) |  |  | AWS secret key for SES mail delivery. | `Config/aws.js`, `Config/config.js` |
| `EMAIL_IN_DOMAIN` |  | `inbox.alianhub.com` | Domain of the inbound-email addresses that create tasks. | `Modules/EmailIn/controller.js` |
| `NODEMAILER_ALLOW_SELF_SIGNED` |  |  | Accept self-signed certificates from the SMTP server. | `Modules/service.js` |
| `NODEMAILER_EMAIL` |  | `your@email.com` | SMTP username / From address. | `Config/config.js` |
| `NODEMAILER_HOST` |  | `smtp.gmail.com` | SMTP host. | `Config/config.js`, `Modules/Instance/controller.js` |

## Storage

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `CLOUD_STORAGE_ENC_KEY` (secret) |  |  | Key used to encrypt cloud-storage provider credentials at rest. | `Modules/CloudStorage/helpers/cloudCrypto.js` |
| `IAM_ENDPOINT` |  | `https://iam.wasabisys.com` | Wasabi IAM endpoint used when provisioning per-company storage users. | `Config/aws.js` |
| `S3_CONNECTION_TIMEOUT_MS` |  |  | Connection timeout for S3-compatible storage calls. | `Config/config.js` |
| `S3_SOCKET_TIMEOUT_MS` |  |  | Socket timeout for S3-compatible storage calls. | `Config/config.js` |
| `STORAGE_TYPE` | yes | `wasabi` | wasabi \| server | `Config/config.js`, `Modules/CloudStorage/controller.js` +27 |
| `TRACKSHOT_RETENTION_DAYS` |  | `180` | Days to keep time-tracker screenshots before deletion. | `Modules/storage/server/helpers/bucket.helper.js`, `Modules/storage/wasabi/controller.js` |
| `USERPROFILEBUCKET` |  | `your-bucket-name` | Bucket that holds user profile pictures. | `Config/aws.js`, `Config/config.js` +1 |
| `WASABIENDPOINT` |  | `https://s3.wasabisys.com` | Wasabi S3 endpoint. | `Config/aws.js` |
| `WASABI_ACCESS_KEY` (secret) |  |  | Wasabi access key. | `Config/aws.js` |
| `WASABI_REGION` |  | `us-east-1` | Wasabi region. | `Config/aws.js` |
| `WASABI_SECRET_ACCESS_KEY` (secret) |  |  | Wasabi secret access key. | `Config/aws.js` |
| `WASABI_USERID` |  |  | Wasabi account user id used for IAM policies. | `Config/aws.js` |

## Ai

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AGENT_ACT_BUDGET_MS` |  | `300000` | Wall-clock budget for the act phase of an agent run, added to the model timeout when sizing the job lock (default 5 min). | `Modules/Agents/engine/timeouts.js` |
| `AI_API_KEY` (secret) |  |  | API key for the configured LLM provider (LLM_PROVIDER). | `Config/config.js` |
| `AI_MAX_TASK_MINUTES` |  |  | Upper bound, in minutes, for a task the AI project generator may plan. | `Modules/AIProjectGenerator/planRules.js` |
| `AI_MODEL` |  | `gpt-4.1` | recommended: gpt-4o or gpt-4.1 for best instruction-following on complex plans | `Config/config.js` |
| `AI_REPLAY` |  | `agent` | Which model calls keep a replay record (prompt, raw response, model, revisions; redacted): off, agent (agent runs only) or all. | `Modules/AICore/replay.js` |
| `AI_REPLAY_RETENTION_DAYS` |  | `30` | Days a replay record is kept before the TTL index deletes it. | `Modules/AICore/replay.js` |
| `ANTHROPIC_API_KEY` (secret) |  |  | Anthropic API key, used when LLM_PROVIDER is anthropic. | `Modules/AICore/llmProvider/anthropicProvider.js` |
| `ANTHROPIC_MODEL` |  | `claude-sonnet-4-5-20250929` | current Sonnet 4.5 model id | `Modules/AICore/llmProvider/anthropicProvider.js` |
| `ANTHROPIC_TIMEOUT_MS` |  | `600000` | optional: override the Anthropic SDK timeout (default LLM_TIMEOUT_MS, 10 min); the agent job lock is derived from the largest provider timeout | `Modules/Agents/engine/timeouts.js` |
| `DEEPSEEK_API_KEY` (secret) |  |  | DeepSeek API key, used when LLM_PROVIDER is deepseek. | `Config/config.js` |
| `DEEPSEEK_BASE_URL` |  | `https://api.deepseek.com` | optional: override DeepSeek base URL (e.g. for a compatible proxy) | `Modules/AICore/llmProvider/deepseekProvider.js` |
| `DEEPSEEK_MODEL` |  | `deepseek-v4-flash` | deepseek-chat / deepseek-reasoner / deepseek-v4-flash / deepseek-v4-pro (all V4: 1M ctx, up to 384K output) | `Config/config.js` |
| `DEEPSEEK_TIMEOUT_MS` |  | `600000` | optional: override the DeepSeek axios timeout (default: LLM_TIMEOUT_MS for reasoner, 4 min for chat); the agent job lock is derived from the largest provider timeout | `Modules/Agents/engine/timeouts.js` |
| `ESTIMATE_SAMPLES` |  |  | Number of historical tasks sampled when the AI estimates effort. | `Modules/EstimatedTime/aiTaskEstimator.js` |
| `ESTIMATE_TARGET` |  |  | Target accuracy band the AI estimator calibrates against. | `Modules/EstimatedTime/aiTaskEstimator.js` |
| `LLM_MAX_TOKENS_PLAN` (secret) |  | `32000` | cap for the planning call (raise if you see "ran out of output token budget") | `Modules/AIProjectGenerator/controller.js` |
| `LLM_PRICING` |  |  | JSON map of model name to per-token prices used to report AI spend. | `Modules/AICore/usage.js` |
| `LLM_PROVIDER` |  |  | openai \| anthropic \| deepseek | `Modules/AICore/llmProvider/index.js`, `Modules/Agents/budget.js` |
| `LLM_REGION` |  |  | optional: the inference region the provider is pinned to; shown read-only in the agent settings (GET /api/v2/agents/settings) | `Modules/Agents/budget.js` |
| `LLM_TIMEOUT_MS` |  | `600000` | Model call timeout every provider inherits unless overridden per provider (default 10 min). The agent job lock is model timeout + AGENT_ACT_BUDGET_MS + 2 min. | `Modules/Agents/engine/timeouts.js` |
| `OPENAI_TIMEOUT_MS` |  | `600000` | optional: override the OpenAI axios timeout (default: LLM_TIMEOUT_MS for reasoning/gpt-5, 4 min for others); the agent job lock is derived from the largest provider timeout | `Modules/Agents/engine/timeouts.js` |

## Calling

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `TURN_CREDENTIAL_TTL_SECONDS` (secret) |  | `3600` | Lifetime of TURN credentials minted from TURN_STATIC_AUTH_SECRET. | `Modules/Calls/iceConfig.js` |

## Integrations

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `APIKEY` (secret) |  |  | Firebase web API key used by the server for push notifications. | `Config/config.js` |
| `APPID` |  |  | Firebase app id. | `Config/config.js` |
| `AUTODOMAIN` |  |  | Firebase auth domain. | `Config/config.js` |
| `GITHUB_BASE_OAUTH_URL` |  | `https://github.com/login/oauth` | authorize + access_token base | `Modules/githubOAuth/controller.js` |
| `GITHUB_CLIENT_ID` |  |  | GitHub OAuth app client id. | `Modules/githubOAuth/controller.js` |
| `GITHUB_CLIENT_SECRET` (secret) |  |  | GitHub OAuth app client secret. | `Modules/githubOAuth/controller.js` |
| `GITHUB_OAUTH_REQUIRED` |  | `true` | "true" requires server-side verification via GitHub /user (recommended) | `Modules/Auth/controller/authHelpers.js` |
| `GITHUB_TOKEN` (secret) |  |  | Token used to read release notes from GitHub for the changelog page. | `Modules/Changelog/controller.js` |
| `GITLAB_BASE_API_URL` |  | `https://gitlab.com/api/v4` | used to verify the identity (/user) | `Modules/Auth/controller/authHelpers.js` |
| `GITLAB_BASE_OAUTH_URL` |  | `https://gitlab.com/oauth` | authorize + token base | `Modules/gitlabOAuth/controller.js` |
| `GITLAB_CLIENT_ID` |  |  | GitLab OAuth app client id. | `Modules/gitlabOAuth/controller.js` |
| `GITLAB_CLIENT_SECRET` (secret) |  |  | GitLab OAuth app client secret. | `Modules/gitlabOAuth/controller.js` |
| `GITLAB_OAUTH_REQUIRED` |  | `true` | "true" requires server-side verification via GitLab /user (recommended) | `Modules/Auth/controller/authHelpers.js` |
| `GOOGLE_CLIENT_ID` |  |  | used for token exchange AND id-token verification | `Modules/googleOAuth/controller.js` |
| `GOOGLE_CLIENT_SECRET` (secret) |  |  | Google OAuth client secret. | `Modules/googleOAuth/controller.js` |
| `GOOGLE_OAUTH_CLIENT_ID` |  |  | Google OAuth client id accepted when verifying Google sign-in tokens. | `Modules/Auth/controller/authHelpers.js` |
| `GOOGLE_OAUTH_REQUIRED` |  | `true` | "true" rejects logins that fail id-token verification (recommended) | `Modules/Auth/controller/authHelpers.js` |
| `GOOGLE_OAUTH_URL` |  | `https://oauth2.googleapis.com/token` | Google token endpoint | `Modules/googleOAuth/controller.js` |
| `MEASUREMENTID` |  |  | Firebase analytics measurement id. | `Config/config.js` |
| `MESSAGINGSENDERID` |  |  | Firebase messaging sender id. | `Config/config.js` |
| `PROJECTID` |  |  | Firebase project id. | `Config/config.js` |
| `SERVICE_FILE` |  | `./firebase-adminsdk.json` | Path to Firebase service account JSON (must be inside project root) | `Config/config.js`, `utils/safeServiceFile.js` |
| `STORAGEBUCKET` |  |  | Firebase storage bucket. | `Config/config.js` |

## Billing

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `FREE_COMPANY_COUNT` |  |  | Companies a user may create on the free plan. | `Modules/Company/controller.js` |
| `PAYMENTMETHOD` |  |  | Payment provider used at sign-up (chargebee or paddle). | `Modules/Company/controller.js` |

## Security

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AUDIT_EXPORT_MAX_ROWS` |  | `100000` | Most rows one audit-log CSV export streams; a last row says when the filter held more. Can only lower the hard cap. | `Modules/Audit/controller.js` |
| `AUDIT_RETENTION_DAYS` |  |  | Days to keep audit-log entries before the recorder prunes them. | `Modules/Audit/recorder.js` |
| `AUTH_RATE_LIMIT_BLOCK_MS` |  | `1800000` | How long an IP stays blocked after too many failed logins. | `Modules/Auth/helper.js` |
| `AUTH_RATE_LIMIT_MAX_ATTEMPTS` |  | `5` | Failed login attempts allowed per window before blocking. | `Modules/Auth/helper.js` |
| `AUTH_RATE_LIMIT_WINDOW_MS` |  | `900000` | Window over which failed login attempts are counted. | `Modules/Auth/helper.js` |
| `CORS_ORIGINS` |  | `http://localhost:8080` | Extra origins allowed by CORS, comma-separated (WEBURL and APIURL are always allowed). | `utils/cors.js` |
| `DISABLE_PERMISSION_ENFORCEMENT` |  |  | Set to true to turn the permission guard into log-only mode. Never in production. | `Config/permissionGuard.js` |
| `ENDPOINT_KEY` (secret) |  |  | Shared key expected by the internal service endpoints. | `Config/config.js` |
| `GLOBAL_RATE_LIMIT_PER_MIN` |  |  | Requests per minute allowed per IP by the global rate limiter. | `index.js` |
| `HELMET_ENABLED` |  |  | Send the helmet security headers (default true). | `index.js` |
| `INSTANCE_ADMIN_KEY` (secret) |  |  | Shared key that grants the instance-admin API without a product-owner session. | `Modules/Instance/guard.js` |
| `MEMBERSHIP_CACHE_TTL_SECONDS` |  | `60` | Seconds a user-company membership check is cached before MongoDB is asked again. | `Config/jwt.js` |
| `PRECOMPANYKEY` (secret) | yes | `your_preset_key` | Random key for the preset company routes, presented in the x-preset-key header; generated at setup. | `Config/config.js` |
| `SOCKETIO_ADMIN_PASSWORD_HASH` (secret) |  | `$2a$12$HHemhzmTrC8Dmf2Gd9v/Teo9oiCxfYJb.St3HKMBx1L3wJmZLeX5u` | bcrypt hash of the Socket.IO admin UI password; the UI stays off while unset. | `socket/socketinit.js` |
| `SOCKETIO_ADMIN_USERNAME` |  | `alian` | Username for the Socket.IO admin UI; the UI stays off while unset. | `socket/socketinit.js` |
| `WEBHOOK_ALLOWED_PRIVATE_HOSTS` |  |  | Private hosts webhooks may post to: exact hostnames or CIDR ranges, comma-separated (empty by default, so every private host is refused). Also editable under Instance > Settings > Security. Link-local and cloud metadata addresses stay blocked, and a range can be no wider than /8 (IPv4) or /32 (IPv6). | `Modules/Webhooks/helpers/privateHostAllowlist.js` |

## Logging

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `FATAL_FLUSH_TIMEOUT_MS` |  | `3000` | Upper bound, in milliseconds, that a fatal crash waits for flushers (log files, telemetry exporter, crash report mail) before the process exits with 1. | `Config/processGuards.js` |
| `LOG_DATE_PATTERN` |  | `YYYY-MM-DD` | Date pattern appended to rotated log file names. | `Config/loggerConfig.js` |
| `LOG_DIR` |  | `log` | Directory that receives the rotated log files. | `Config/loggerConfig.js`, `Modules/Instance/logsPath.js` |
| `LOG_FORMAT` |  | `text` | Log file record format: text (one formatted line) or json (one { ts, level, label, requestId, traceId, message } record per line). | `Config/logFormat.js`, `Config/loggerConfig.js` |
| `LOG_MAX_FILES` |  | `14d` | Retention window for rotated log files (winston-daily-rotate-file). | `Config/loggerConfig.js` |
| `LOG_MAX_SIZE` |  | `20m` | Size at which a log file rotates. | `Config/loggerConfig.js` |
| `OTEL_EXPORTER_OTLP_ENDPOINT` |  |  | OTLP/HTTP collector base URL (traces go to <endpoint>/v1/traces). Unset keeps the OpenTelemetry SDK and exporter off; trace ids are still recorded. | `Config/telemetry.js` |
| `OTEL_SERVICE_NAME` |  | `alianhub` | service.name reported on exported spans. | `Config/telemetry.js` |

## Limits

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `BODY_LIMIT` |  | `2mb` | Maximum JSON/urlencoded/raw request body (body-parser size string). | `index.js` |
| `CLOUD_STORAGE_MAX_IMPORT_BYTES` |  |  | Largest file the cloud-storage importer will copy into a project. | `Modules/CloudStorage/controller.js` |
| `FORM_MAX_FILES` |  | `3` | Maximum attachments on one public form submission. | `Modules/Forms/helpers/formUpload.js` |
| `FORM_MAX_FILE_BYTES` |  |  | Maximum size of one public form attachment. | `Modules/Forms/helpers/formUpload.js` |
| `MAX_IMAGE_FILE_BYTES` |  | `26214400` | Largest image accepted for processing. | `utils/imageGuard.js` |
| `MAX_IMAGE_PIXELS` |  | `50000000` | Largest image, in pixels, accepted for processing (decompression-bomb guard). | `utils/imageGuard.js` |
| `UPLOAD_MAX_FILES` |  | `20` | Maximum files per upload request. | `utils/uploadConfig.js` |
| `UPLOAD_MAX_FILE_BYTES` |  |  | Maximum size of one uploaded file. | `utils/uploadConfig.js` |

## Jobs

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `AGENT_ALERTS_INTERVAL_MINUTES` |  | `15` | Minutes between runs of the agent rate-alert evaluator (error rate, approval rate, cost forecast, queue age). | `Modules/Agents/alerts.js` |
| `AGENT_PROPOSAL_STUCK_MINUTES` |  | `10` | Minutes an agent proposal may sit in 'applying' before the reaper fails it. | `Modules/Agents/proposals.js` |
| `AUTOMATION_CONCURRENCY` |  | `5` | Automation runs processed in parallel. | `Modules/Automations/engine/index.js` |
| `AUTOMATION_ENGINE` |  | `true` | Set to false to stop the automation engine from starting. | `Modules/Automations/engine/index.js` |
| `AUTOMATION_EVENT_LOG` |  |  | Set to true to record every domain event in automation_event_log for debugging automations. | `event/domainEventBus.js` |
| `AUTOMATION_QUEUE_DRIVER` |  | `agenda` | Queue driver for automation runs (agenda or memory). | `Modules/Automations/engine/index.js`, `Modules/Instance/controller.js` |
| `CRON_TZ` |  | `UTC` | IANA time zone for scheduled jobs (reminders, digests). | `Modules/GeneralReminders/scheduler.js`, `Modules/Instance/controller.js` +1 |
| `GENERAL_REMINDERS_CRON` |  |  | Cron expression for the general reminders scheduler. | `Modules/GeneralReminders/scheduler.js` |
| `NOTIFICATIONACTIVETIME` |  |  | Minutes a user counts as active for notification batching. | `Modules/notification/prepare-notification-data/user-controllerV2.js` |

## Frontend

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `VUE_APP_AFFILIATEON` |  |  | Show the affiliate programme entry points. | `frontend/src/App.vue`, `frontend/src/config/env.js` |
| `VUE_APP_APIKEY` (secret) |  |  | Firebase web API key (push notifications). | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_APPID` |  |  | Firebase app id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_AUTHDOMAIN` |  |  | Firebase auth domain. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_CUSTOMFIELDID` |  |  | Custom field id used by the support product link. | `frontend/src/components/organisms/TaskDetailOverlay/TaskDetailPanel.vue` |
| `VUE_APP_CUSTOMFIELDPRODUCTID` |  |  | Custom field id that stores the product on support tickets. | `frontend/src/components/organisms/TaskDetailOverlay/TaskDetailPanel.vue` |
| `VUE_APP_GITHUB_BASE_OAUTH_URL` |  |  | GitHub OAuth authorize URL. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GITHUB_CLIENT_ID` |  |  | GitHub OAuth client id for the login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GITLAB_BASE_OAUTH_URL` |  |  | GitLab OAuth authorize URL. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GITLAB_CLIENT_ID` |  |  | GitLab OAuth client id for the login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_GOOGLE_CLIENT_ID` |  |  | Google OAuth client id for the login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_GITHUB_LOGIN` |  |  | Show the GitHub login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_GITLAB_LOGIN` |  |  | Show the GitLab login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_GOOGLE_LOGIN` |  |  | Show the Google login button. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_IS_SSO_LOGIN` |  |  | Show the SSO login entry point. | `frontend/src/config/publicConfig.js` |
| `VUE_APP_MEASUREMENTID` |  |  | Firebase analytics measurement id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_MESSAGINGSENDERID` |  |  | Firebase messaging sender id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_PROJECTID` |  |  | Firebase project id. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_STORAGEBUCKET` |  |  | Firebase storage bucket. | `frontend/src/config/firebaseConfig.js` |
| `VUE_APP_STORAGE_TYPE` |  |  | Storage backend the web app assumes (server or wasabi); mirror STORAGE_TYPE. | `frontend/src/config/env.js`, `frontend/src/config/publicConfig.js` |
| `VUE_APP_SUPPORT_MAIL` |  |  | Mailbox shown for support requests. | `frontend/src/views/Projects/Comments/Comments.vue` |
| `VUE_APP_SUPPORT_PROJECTID` |  |  | Project id that receives support tickets raised from the app. | `frontend/src/components/organisms/TaskDetailOverlay/TaskDetailPanel.vue` |
| `VUE_APP_USEREMAIL` |  |  | Default account email pre-filled on demo builds. | `frontend/src/views/Projects/Comments/Comments.vue` |

## General

| Variable | Required | Default | Description | Read by |
|---|---|---|---|---|
| `ALIANHUB_DOCKER` |  |  | Set by the Docker image so defaults suit a container (storage under /app/storage). | `Modules/Instance/controller.js` |
| `BACKUP_DIR` |  | `backups` | Directory where in-app backups are written (default backups/). | `Modules/Instance/backups.js` |
| `CRON_ENABLED` |  |  | Run the scheduled jobs in this process (default true; set false on extra replicas). | `Modules/Instance/controller.js`, `index.js` |
| `DEMO_EMAIL` |  |  | Email of the demo account shown on the login page when DEMO_MODE is on. | `Modules/Admin/common/controller.js` |
| `DEMO_MODE` |  | `false` | Set to true to show the demo login banner and credentials. | `Modules/Admin/common/controller.js` |
| `DEMO_PASSWORD` (secret) |  |  | Password of the demo account shown when DEMO_MODE is on. | `Modules/Admin/common/controller.js` |
| `ERRORRECIVEREMAIL` |  | `admin@yourdomain.com` | Email to receive error alerts | `Config/config.js` |
| `HEALTH_DB_TIMEOUT_MS` |  |  | How long /health waits for the database ping before reporting it down (default 2000). | `Modules/Instance/health.js` |
| `MIGRATIONS_AUTO` |  |  | Apply pending migrations at boot (default true; false only reports them). | `Modules/Instance/controller.js` |
| `MONGODB_URL` | yes |  | mongo connection string (no trailing slash) | `Config/config.js`, `Modules/AICore/persistence.js` +8 |
| `NODEMAILER_EMAIL_PASSWORD` (secret) |  |  | Use app password for Gmail | `Config/config.js` |
| `NODEMAILER_PORT` |  | `587` | 587 (STARTTLS) or 465 (SSL) | `Config/config.js` |
| `NOOFPRESETCOMPANY` |  | `10` | Number of preset companies seeded at first run. | `Config/config.js` |
| `RESEND_API_KEY` (secret) |  |  | Get from resend.com | `Config/config.js`, `Modules/Instance/controller.js` |
| `RESEND_FROM_EMAIL` |  | `onboarding@resend.dev` | Use verified domain in production | `Config/config.js` |
| `TRANSLATE_API_KEY` (secret) |  |  | Machine-translation key used by scripts/i18n-backfill.js; unset means English copies are written for review. | `scripts/i18n-backfill.js` |
