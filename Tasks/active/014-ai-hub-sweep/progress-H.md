# 014 — backend fixes (agent H)

Branch `fix/ai-hub-sweep`. Every row has a test in `tests/agent-*.test.js`; `npm test` and `npm run lint` green at each commit.

| # | Defect | What changed | Test | Evidence |
|---|---|---|---|---|
| 1 | Rule-triggered runs bypass agent controls | `Modules/Automations/engine/actions/runAgent.js` rewritten: resolves the agent named in `config.agent` (id or name), `runs.canStart({trigger:'rule'})`, project scope, `runs.create` → `runs.executeSkill` with an agent actor on behalf of the rule's author. No agent named / unknown / paused / capped / limited / out of scope → deterministic error recorded on the automation run step (no retries). Output `{ runId, verdict: applied|proposed|skipped|stopped }`. Builder schema now `agent` (text) + `skill` (every executable slug); sentence rule accepts `run the <skill> agent as "<name>"`. | `agent-automation-run.test.js` (7) | refusal messages: "This rule does not name an agent…", `No agent named "X"…`, "Code Reviewer cannot run: Agent is paused (pause_all)." |
| 5 | Approve/decline not atomic | `proposals.js`: `setStatus(..., { onlyIf: 'pending' })` claims the row as `applying` before any perform; decline is the same conditional write. Loser gets 409 "Proposal is being applied." / "Proposal is already approved." Gate check happens before the claim. | `agent-proposal-atomic.test.js` (6) | concurrent approve → perform called once |
| 9 | Approval bypasses allowedActions | `proposals.approve` loads the agent and passes `allowedActions` to every `actions.perform`; a deleted agent → 409 "This agent was deleted — decline the proposal instead." | same suite | `perform` called with `allowedActions: ['task.get','task.comment']` |
| 7 | Stopped runs resurrected; no reaper | `runs.patch/finish` take `onlyIf`; every terminal write in `executeSkill` is conditioned on `status: running` and returns `{status:'abandoned'}` (no proposal filed) when stop/pause-all won. `runs.reapStale(companyId)` marks `running` → `failed` "server restarted"; `Modules/Agents/init.js` runs it for every company at boot. | `agent-run-lifecycle.test.js` (#7 block, 3) | stop() during the skill → row stays `stopped by u2`, `proposals.create` not called |
| 10 | Skipped counted as done | `runs.STATUS.SKIPPED = 'skipped'` (+ `TERMINAL`); skill skips end `skipped` with the reason in `outcome`. `runs.countsByStatus` and `GET /agents/runs/summary` return `counts` per status. `agentFit.historyFor` excludes skipped from `finished`/`good` and reports `skipped`. | lifecycle (#10 block, 2), `agent-fit.test.js` ("skipped runs are neither clean nor failed") | counts `{ …, done:1, skipped:2, failed:1 }` |
| 8 | Schedule/rate limit never enforced | `runs.canStart` is async and, given `companyId`, counts today's (UTC) runs for the agent: "Daily run limit reached (2 of 2 today)." Controller awaits it. `schedule` still stored; comment in `agentPatchFields` says nothing consumes it (agent I hides the field). | lifecycle (#8 block, 3), `agent-delete.test.js` (schedule note) | — |
| router | Router picked Code Reviewer for tasks with no PR | `Modules/Agents/taskInputs.js` (`inputsOf` → `{ prUrl, publicUrl, briefChars }`) attached as `task.inputs` by `GET /agents/routable` (body fields projected then stripped). `agentFit.js`: `SKILL_INPUTS` — `pr.summary|risk.flags` need a PR link, `qa-review|qa.review` a public URL, `brief.parse|project.plan` ≥ 40 chars, `digest.*|risk.today` never per task; an agent whose every known skill is unmet is ineligible ("This task lacks what it needs — …"); `routeTasks` refuses with "needs a person — <reason>". | `agent-fit.test.js` (5 new), `agent-task-inputs.test.js` (3) | — |
| 15 | No way to delete an agent | `DELETE /api/v2/agents/:id` → `controller.deleteAgent`: owner/admin or `agent.ownerId`; 409 while a run is `running`/`queued`; soft delete (`deletedStatusKey:1`, `deletedBy`, `paused`), runs/proposals kept; `agentAudit.recordAgentDeleted` (`agent.deleted`); socket emit. `/api/v2/agents` prefix already in `Config/setMiddleware.js`. | `agent-delete.test.js` (5) | — |
| 17 | `refusals` shape | Stays a number: `executeSkill` `$inc`s `refusals` per `RefusedError` and pushes `{ action, ok:false, refused, auditId }` into `actions`; run payload unchanged otherwise. Agent I reads `run.refusals` as a number. | lifecycle (#9/#17 block) | row `refusals: 1`, `typeof === 'number'` |

Also: `executeSkill` now consults finding memory (dedupe that only the automation path had) for every trigger; refused direct actions no longer abort the run.

Shared-file edits outside Modules/Agents + runAgent.js + tests: `Modules/Automations/helpers/sentenceRules.js` (optional `as "<agent>"`), `tests/fixtures/fakeMongo.js` (new in-memory CRUD fake).
