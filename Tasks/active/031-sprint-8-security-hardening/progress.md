# Progress: Sprint 8 — security hardening

## Checklist
One pull request per step; tick with the merge commit.

- [x] Step 1: Server-side permission enforcement for browser sessions, after bringing the backend catalogue to parity with t — code complete: #740 `9ef9bf2e` (build 214), #743 `e3b1e540` (218), #749 `9d4dd00c` (222), #751 `f669c689` (224); the two-week report-only period runs per workspace once the owner turns it on
- [x] Step 2: Service identities for the engine, workers, indexer and router; step-scoped short-lived credentials minted per — step credentials #760 `a8a7b110` (build 234), the secrets store #759 `72c43bfc` (build 235, migration 031), per-tenant provider keys #778 `f7d0a62b` (build 249, slice 9b)
- [x] Step 3: Agent egress through an allow-listing proxy per tenant. — #758 `2e186c56` (build 236), built as an in-app gateway
- [x] Step 4: Audit as append-only with a per-tenant hash chain; audit retention at least as long as run retention; tainted- — chain and retention done in #750 `94f6d206` (build 225); tainted-run marking and approval routing in #757 `8d3a292e` (build 232)
- [ ] Step 5: Http-only session cookies and a content security policy, which is frontend work with the socket and refresh fl — http-only cookies #780 `f4ac85ee` (build 251); the content security policy (slice 12) in progress
- [x] Interface: Accounts → tokens (extend) — #744 `88246531` (build 219), step-scoped credentials card in #760 (build 234); owner and member sweep still to record
- [x] Interface: Instance console → egress allowlist (new) — #758 `2e186c56` (build 236); owner sweep still to record
- [x] Interface: Instance console → enforcement (new) — #754 `9fc7a2ba` (build 228); owner sweep still to record
- [x] Interface: Audit log (extend) — #750 `94f6d206` (build 225); owner and member sweep still to record
- [ ] Defects closed: #2, #9, #20
- [ ] Exit gate met and gates green

## Log
| Date | Entry |
|---|---|
| 2026-09-10 | Filed from `docs/AI-PLATFORM-ARCHITECTURE.md` (sprint 8) |
| 2026-09-16 | Started, in parallel with Sprint 7. Planned against `beta` at `cb9ce0e9` into the fourteen pull requests listed under Decisions in task.md; none of the five steps existed, and the planning pass found two defects that ship first as slices 0a and 0b. The owner settled per-workspace enforcement and how erasure treats the audit chain. Slices 0a, 0b and 1 started, each in its own worktree. |
| 2026-09-16 | Slice 0b merged: #737 `e691b378` (build 212), AI-generated lists are parsed as JSON with a shape check. Slice 0a merged: #738 `d781f3a8` (build 213), tasks no longer carry a session token and migration 028 clears stored values. Slice 1 merged: #740 `9ef9bf2e` (build 214), the server permission evaluator agrees with the web app without refusing anything `beta` allowed; the differences that remain are recorded with tests for the report-only slice. An independent review found one new API-token refusal (request ids compared by case) and a migration that would have changed what the web app shows; both were fixed before merge and the migration was dropped, leaving 029 free. |
| 2026-09-17 | Owner decisions: an audit row that changes is recorded as a new chained row; the chain hash is keyed with a new `AUDIT_CHAIN_KEY`; existing API tokens without an expiry keep working for 30 days after mandatory expiry turns on. Slice 2 (report-only enforcement for web sessions, migration 029) and slice 7 (mandatory token expiry and explicit scopes) started. |
| 2026-09-17 | Slice 2 merged: #743 `e3b1e540` (build 218), browser sessions pass through the permission check in off, report and enforce modes, the mode set per workspace with the instance value as default, would-be denials recorded after the response without bodies or paths; its review found two refusals enforce would have added (invitation acceptance, a member's own preferences) and both were fixed first. Slice 7 merged: #744 `88246531` (build 219), mandatory expiry and explicit scopes behind `API_TOKEN_STRICT`, a per-token 30-day grace, MCP reads now checking the read scope. Slice 3 merged: #749 `9d4dd00c` (build 222), every task write mapped to its key and judged through the enforcement mode, ids resolved the way the handlers resolve them. Owner decisions: a workspace shows as ready to enforce after 14 days without a would-be denial; owners and admins get a workspace list of tokens still needing an expiry (slice 7b). |
| 2026-09-18 | Slice 3b merged: #751 `f669c689` (build 224), task writes stay in the authorised company and change only their own fields, no stub tasks on update; the adversarial review found nothing that breaks it and its further findings became slice 3c. Slice 5 merged: #750 `94f6d206` (build 225), the per-tenant audit hash chain, after three review rounds fixed eleven defects (stored text normalised the way BSON stores it, only verified amendments applied on every read path including export, a chained row never edited in place whatever the flag, the head mirrored from what each process wrote, undo refusing before an inverse it cannot record). Slices 3c, 4 (enforcement console) and 7b started. |
| 2026-09-18 | Slice 7b merged: #753 `3bdf07cb` (build 227), owners and admins see the workspace's tokens that still need an expiry: name, owner, deadline and last use, never the token. Slice 4 merged: #754 `9fc7a2ba` (build 228), the instance console's enforcement tab: the mode per workspace, would-be denials grouped by permission, and the 14-quiet-days readiness mark. |
| 2026-09-19 | Owner decisions: the secrets store is keyed with a new `SECRETS_KEY` environment variable; agent egress is an in-app gateway, and an empty allowlist keeps today's behaviour. #762 `e8e36aac` (build 230) fixed the Docker image build, which had stopped when the frontend began sharing a second backend module, and added a conventions test that ties every such alias to its `COPY` line. Slice 3c merged: #756 `7a1e632c` (build 231), task index queries are validated and the actor and counts come from stored data; merged after three review rounds, each round's findings fixed and covered by tests first. Slice 6 merged: #757 `8d3a292e` (build 232), runs that read outside content are marked, and their risky actions go to approval, behind `AGENT_TAINT_ROUTING`; the run view shows the mark. Slices 8 (step credentials, #760), 9 (secrets store, #759, migration 031) and 10 (egress allowlist, #758) are in review fix rounds; slice 12 (content security policy, `CSP_MODE`, migration 033 if it needs one) started. |
| 2026-09-20 | Slices 8, 9 and 10 merged after an independent review found no blocking defects: #760 `a8a7b110` (build 234) mints step-scoped credentials on claim, re-mints on heartbeat and checks the live step row on every agent action; #759 `72c43bfc` (build 235) keeps integration and webhook secrets by handle with migration 031; #758 `2e186c56` (build 236) gates agent fetches per workspace with an in-app allowlist. Each merged clean after its review fixes; #759 and #758 each needed a beta merge first (locale pending-key unions, ENV regen, and for #758 keeping both the SECRETS and EGRESS_ALLOWLISTS additions to the shared collection/schema registries), with CI re-run green before merge. Review notes kept as follow-ups: vault rows are created before their document write, so a failed write orphans a live unreferenced handle (visible and revocable under Settings → Integrations); `AGENT_EGRESS_ALLOWLIST` reads only `true` while sibling flags accept on/1/yes. Remaining: slice 12 (CSP), slice 11 (http-only cookies) and 9b (per-tenant provider keys through the secrets store). |
| 2026-09-20 | Slice 9b merged: #778 `f7d0a62b` (build 249), per-workspace provider keys through the secrets store behind `TENANT_PROVIDER_KEYS`: one key per provider as a `provider` secret with the handle on the company row, the workspace travelling in an `AsyncLocalStorage` context set by request middleware, the agent graph, queue jobs and the spend meter, adapters resolving at call time with instance-key fallback and fail-closed refusal on an unopenable key. Owners and admins manage keys under Settings → routing policy. Two CI rounds: the provider-names lookup had to move to call time (the adapter require cycle hands back unfinished registry exports), and the new specs needed the established controller mocks and call-log resets. Remaining: slice 12 (CSP) and slice 11 (http-only cookies). |
| 2026-09-20 | Slice 11 merged: #780 `f4ac85ee` (build 251), http-only session cookies behind `SESSION_COOKIE_HTTPONLY`: the JWT middleware, socket handshake and refresh endpoint read the cookie when no explicit header is sent (headers always win), clears match the set domain/path, the sessions list marks the caller's own row, and the web client authenticates by cookie (interceptors omit empty headers, refresh relies on server rotation plus the reuse-grace window, router and app use the live user fetch and stored user id, logout is server-driven, socket connects with credentials). Response bodies still carry tokens for the tracker. The e2e/integration harness runs with the flag on. Three CI rounds: an emptied `Bearer ` header arrives trimmed to bare `Bearer` and had to fall through to the cookie like an absent header (found by e2e, proven in a local browser); the new specs needed the credential helper kept and a flag-off compat server. Remaining: slice 12 (CSP). |
| 2026-09-21 | The review rounds for slices 8 and 10 finished after those slices merged. #782 `754c05f0` (build 253) completes slice 8: ids compared without regard to case, an agent that cannot be read granted nothing, a lapsed lease no longer renews its credential, the run must still be running, a failed mint or note write hands the step back, the credential cannot be copied off the actor, the credential id enforced and kept out of every workflow API answer. #784 `0512f955` (build 254) completes slice 10: the guard scan catches every outbound request form and SDK client, the cached empty-list answer expires after 30 seconds, wildcards under wildcard-DNS and shared-hosting domains are refused, and the console saves are version-checked and paged. Each rule was shown to fail its test when removed. Slice 12 (content security policy, #785) is in its third review round; its upload rule serves stored files as downloads unless they are a safe inline type, in every mode. |

## Last step
Steps 1 to 4 are merged, with the review follow-ups for slices 8 and 10 (builds 253 and 254). Slice 12 (content security policy, #785) is the last slice; it is in review. Open owner decisions: a maximum token lifetime, and how the Google Drive picker works under an enforced policy.
