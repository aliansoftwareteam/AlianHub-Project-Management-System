# Progress: Sprint 7 — knowledge and retrieval

## Checklist
One pull request per step; tick with the merge commit.

- [x] Step 1: One retrieval interface with hybrid lexical and vector search; the lexical implementation ships first on the f — lexical done in #739 `45f20831` (build 215); embeddings and hybrid search #761 `b56dec6a` (build 229), under the per-workspace mode `hybrid`
- [x] Step 2: Ingestion off the event bus, extended to page, comment, attachment and memory events: extract, chunk on struct — pages #745 (build 221), comments and transcripts #747 (223), attachments and guides #783 `e3b0e6ce` (256), memory #787 `4cbaeaca` (258); erasure path in #788 `af842580` (259)
- [x] Step 3: Sources brought in order of value: page bodies, comments, meeting transcripts, uploaded files through text ext — pages, comments, transcripts, workspace pages (#745, #747), files and project guides #783 (build 256), the performance read action #746 (220)
- [ ] Step 4: The vector adapter for hosted deployments behind the interface; agent-scoped memory as a distinct scope in the — agent-scoped memory #787 `4cbaeaca` (build 258); the hosted vector adapter (slice 7) in progress
- [ ] Step 5: The regular-expression path in Ask is retired.
- [x] Interface: Instance console → knowledge sources (new) — #788 `af842580` (build 259); owner sweep still to record
- [x] Interface: Ask → citations (extend) — #742 `fea5e063` (build 217); owner and member sweep still to record
- [ ] Exit gate met and gates green

## Log
| Date | Entry |
|---|---|
| 2026-09-10 | Filed from `docs/AI-PLATFORM-ARCHITECTURE.md` (sprint 7) |
| 2026-09-16 | Started, in parallel with Sprint 8. Planned against `beta` at `cb9ce0e9` into the twelve pull requests listed under Decisions in task.md; none of the five steps existed. The owner settled three questions (departed members, agent drafts, erasure and the audit chain). Slices 0 (Ask private-sprint sources) and 1 (retrieval interface) started, each in its own worktree. |
| 2026-09-16 | Slice 0 merged: #735 `ff21c84c` (build 210), Ask's task sources apply the private-sprint rule through the shared `hiddenSprintFilter`. Slice 1 merged: #739 `45f20831` (build 215), `Modules/Knowledge` adds the retrieval interface, a per-caller visible set re-checked against live rows after ranking, and a MongoDB text-index adapter with an escaped-regex fallback, behind `KNOWLEDGE_RETRIEVAL` (off by default; with it off Ask runs exactly as before); migration 024 adds the pages text index. An independent review found no access defects, but rule tests that still passed with their rule removed and a malformed company opt-out that read as on under `all`; both were fixed before merge, each rule test proven to fail without its rule. |
| 2026-09-17 | Owner decision: embeddings come from OpenAI, which unblocks slice 6 once slice 2 merges. Slice 2 (chunk store and page ingestion off the event bus, `KNOWLEDGE_INDEXER` off by default, migration 025) and slice 9 (Ask "why this answer" panel) started. |
| 2026-09-17 | Slice 9 merged: #742 `fea5e063` (build 217), Ask's "why this answer" panel with a reason per passage. Slice 5 merged: #746 `9988dbf7` (build 220), `performance.read` behind `AGENT_PERFORMANCE_READ`, every metric on the same UTC days after its review. Slice 2 merged: #745 `3a9a16a8` (build 221), the chunk store and page ingestion with migration 025; its review found no access widening but five result-loss defects (agent drafts indexed by title only, a backfill racing a restore, departures and erasures not sticking, long pages crowding others), all fixed first. Slice 3 merged: #747 `f8d489f6` (build 223), comments, transcripts and workspace pages with migration 026; its review's load and robustness findings fixed first, and the owner's erasure decision applied (a person's private pages and their comments leave the index; transcripts stay). |
| 2026-09-18 | Owner decisions: an agent retrieves as the run's starter limited to the agent's projects; hosted vectors live in MongoDB Atlas Vector Search inside each tenant's database; the owner writes the held-out question set. Every Sprint 7 decision is settled. Slice 6 (embeddings and hybrid retrieval) started. |
| 2026-09-19 | Slice 6 merged: #761 `b56dec6a` (build 229), OpenAI embeddings and hybrid lexical plus vector retrieval, chosen per workspace with the mode `hybrid`; its review had the embedding budget hold under concurrent work, the query embed time out and fall back to lexical, a breaker pause embedding after repeated provider failures, and the agent-draft ranking rule apply to fused results. The first pull request (#755) was closed for a commit subject over 100 characters and its commits moved unchanged to #761. Slice 4 (file text extraction and project guides, which also sets `origin` on every passage for Sprint 8's tainted-run rule) started; it takes migration 032 if it needs indexes, because 031 is held by Sprint 8's open secrets-store pull request. |
| 2026-09-21 | Slice 4 (files and project guides, #783) resumed after the 2026-09-19 usage stop and is in its third review round; it takes migration 032 for the file sweep's index. Slice 8 (agent-scoped memory, `KNOWLEDGE_AGENT_MEMORY`, migration 034 if needed) started. Slice 10 (knowledge console) waits for #783, since it reports per-source figures that include files. |
| 2026-09-21 | Slice 4 merged: #783 `e3b0e6ce` (build 256), project guides and task file text as sources with `origin` on every passage, migration 032, after three review rounds (inflation counted on real bytes, types checked against content, the attachment permission per project, restart-safe claimed sweeps that use their index). Slice 8 merged: #787 `4cbaeaca` (build 258), agent-scoped memory behind `KNOWLEDGE_AGENT_MEMORY`, after three rounds: every source type narrowed to the agent's projects, each note rechecked against the current run for every source it came from, provenance and taint from the stored run, tainted notes starter-only. Slice 10 merged: #788 `af842580` (build 259), the instance console Knowledge tab, after three rounds (canonical ids, audited erasure even on failure, leases for re-index and re-embed, bounded figures, a listed and undoable exclusion). Slice 7 (Atlas Vector Search adapter, `KNOWLEDGE_VECTOR_STORE`) started. |

## Last step
Slices 0–6 and 8–10 merged (builds 210–259). Slice 7 (Atlas adapter) in progress; slice 11 (retire the regex path) waits for the owner's question set. Open owner decisions: what an agent with no projects may retrieve, whether agent runs reach content outside any project, and whether agent notes appear in Ask (all fail closed today).
