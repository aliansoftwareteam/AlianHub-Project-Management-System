# Progress — workstream C

| Date | Step | Result | Evidence |
|---|---|---|---|
| 2026-09-04 | C.1 Navigation | Done · `efd61ee` | Rail = Home · Projects · Inbox · Planner · Chat · AI · Docs · Dash · Time (`Shell/navItems.js`); project More menu grouped Find / Insights / Share & export / Import / Project settings with `ah-pop__label`/`__sep`; five importers open from `components/organisms/ImportDialog/ImportDialog.vue`; "Estimation scale" via `ProjectsV2.estimation_scale`. Lint clean. |
| 2026-09-04 | C.2 + New in project header | Done · `83c29c4` | `views/Projects/components/NewInProjectMenu.vue` (List / Folder, gated on `project_sprint_create` / `project_folder_create`, reuses `SprintFolderInput`), mounted in `ProjectHeader`'s `actions` slot from `Projects.vue` (one-line mount + import). New list navigates to `ProjectSprint`. |
| 2026-09-04 | C.3 Project apps | Done · `fc3c207` | `components/molecules/ProjectAppsList/ProjectAppsList.vue` (row per app, toggle, `AppsV2.<key>_title/_desc`) replaces the icon row in `ProjectsListingSetting.vue` and joins `CreateProjectSidebar.vue` (defaults from `GET /api/v1/projects-apps` or the template); `blankTemplate.js` turns on Tags + Custom fields; `Modules/createProject/apps.js pickKnownApps` lets the client selection win. `tests/create-project-apps.test.js` 5 tests. |
| 2026-09-04 | C.4 Lifecycle · bulk · Trash | Done · `3b2fdc6` `c029f92` `a1039c9` | `frontend/src/utils/lifecycle.js` (`lifecycleOf`, `keyFor`; CJS so root Jest loads it) used by `useProjectLifecycle.js` and `ProjectsListPage.vue`; `bulkTrash` beside `bulkArchive`/`bulkRestore` in `taskMongo/bulk.js`; `ListBulkBar.vue` Archive/Delete behind `ConfirmationSidebar`, mounted in `BoardView.vue`, `OWN_BULK_BAR_VIEWS` += `ProjectKanban`; `Modules/Trash/{rules,controller,routes,init}.js` — `GET /api/v2/trash?kind=`, `PUT /api/v2/trash/:kind/:id/restore`, `DELETE /api/v2/sample-data`; `views/Trash/TrashPage.vue` at `/:cid/trash` via `router/trash/index.js`, rail More → Workspace → Trash. Tests: `lifecycle-mapping` 21, `tasks-bulk-archive-trash` 8, `trash-controller` 13. Real key map: project 2 archived / 1 trashed, sprint 2 archived / 5 closed / 1 trashed, task 1 trashed, {2,3,4,6,7} archived, {5,8} closed. |
| 2026-09-04 | C.5 Onboarding for every role | Done · `4b6253a` `8942548` | `Tour/tourSteps.js` `{shell, project, board, list}` + `screenFor(route)`; `TourComponet.vue` reads steps by screen, gates only on width > 767, resumes/skips per screen; `tests/tour-selectors.test.js` 36 tests (every anchor exists in source, every copy key exists). `composable/useOnboardingChecklist.js` `MEMBER_STEPS` / `ADMIN_STEPS` persisted in `homeChecklist`; `TodayOverdue.vue` drops `isOwnerOrAdmin`; Remove sample data step → `DELETE /api/v2/sample-data`; sample row action relabelled `ProjectsV2.remove_sample`. `navItems.js` tour entry open to all roles. |
| 2026-09-04 | C.7 Dogfood fixes | Done · `4202a79` `7e23909` | `TaskDetailPanel.reflectOwnUpdate` commits `mutateUpdateFirebaseTasks`/`mutateMongoUpdatedTask`/`mutateTypesenseTableTasks` after name/status saves; `Modules/notification/defaults.js ensureNotificationDefaults(companyId, userId)` called from `Company/controller.js`, `CheckInstallStep/createCompany.js` and lazily by `GET /api/v1/notifications/:id` (`tests/notification-defaults.test.js` 4); `isBundledPriorityImage` in `commonFunction.js` used by `PriorityComp.vue`, `SidebarItems.vue`, `composable/index.js`; seed drops `taskPriorities/` paths; `SprintReport.vue` select gets `aria-label`. `first-run-checklist.test.js` repointed at the composable. |
| 2026-09-04 | C.6 i18n discipline | Done (commit below) | `scripts/i18n-check.js` (no deps; loads `export default` locales incl. the `en` spread ones; missing keys per locale; quote-aware template scan for bare text / `title` / `placeholder` / `aria-label`; per-file baseline `scripts/i18n-allowlist.json`), `npm run i18n:check`, `tests/conventions/i18n-check.test.js` 16 tests; `scripts/i18n-backfill.js` (`TRANSLATE_API_KEY` unset → English copy, `<locale>.pending.json` review maps; spread-based ja/ko/ptBr left as is). Before: ar 27/6912, ch/fr/ge/gr/gu/hi/it/ru/spa 2061–2143/6912, ja/ko/ptBr 6912/6912; hardcoded 434 findings / 134 files. After: all 13 at 6915/6915 missing 0; hardcoded 430 / 133 baselined. `CLAUDE.md` Rule 3 added. Commit `b8ef344`. |
| 2026-09-04 | Verification | Backend 98 suites / 1304 tests green (was 91 / 1197); `cd frontend && npm run build` → "Build complete", exit 0; `node scripts/i18n-check.js` exit 0. curl on `PORT=4102` with a dev PAT (issued via `scripts/issue-api-token.js`, revoked after): `GET /api/v2/trash?kind=projects|lists|tasks|docs` 200 `{status:true,data:[…]}`; `POST /api/v2/tasks/bulk {action:"bulkTrash"}` on SPWC-1 → `totals {updated:1}`; `GET …?kind=tasks` lists SPWC-1; `PUT /api/v2/trash/tasks/:id/restore` → `{status:true,statusText:"Restored."}`; list empty again; `DELETE /api/v2/sample-data` → `{projects:0,tasks:0}` (no WELCOME project in the dogfood company); `kind=folders` → 400; no token → 401; `GET /api/v1/notifications/:uid` returns the owner's settings doc created by the lazy default. DB left as found (SPWC-1 `deletedStatusKey 0`, 0 trashed). | see report |
