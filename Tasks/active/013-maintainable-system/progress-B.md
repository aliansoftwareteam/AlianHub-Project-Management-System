# Progress — workstream B

| Date | Step | Result | Evidence |
|---|---|---|---|
| 2026-09-04 | B.1 hygiene | done, one exception | `/storage/` added to `.gitignore`; `scripts/unused-components.js` resolves `@/` and relative imports (with or without `.vue`, through `/* webpackChunkName */` comments) and template-string dynamic imports; baseline = 42 unreferenced SFCs (`plugins/dashboard-old/views/Home.vue` + 41). `.tmp-pr515/` was **not** deleted: `diff -r` against `Modules/Pages` is not empty and it holds a "Workspace Ask" feature (`Modules/Pages/helpers/pageWorkspaceAsk.js`, `components/molecules/Pages/WorkspaceAskPopover.vue`) that exists in no branch or commit — integrator to decide. |
| 2026-09-04 | B.2 lint | done | Root `.eslintrc.cjs` (`eslint:recommended`, node/es2022, `no-unused-vars` warn, `no-empty` allowEmptyCatch, `no-async-promise-executor` warn). `npx eslint .`: 186 errors → **0 errors**, 480 warnings (411 `no-unused-vars`, 69 `no-async-promise-executor`). Hand fixes: 57 `no-undef` (implicit globals in `mongo_helper.js`, `handleNotification.js`, `taskIndex/controller.js`, `notification-count/controller.js`, `err`/`error` mix-ups in `common-storage/*`, `reject` used outside a Promise in 4 controllers, `SCHEMA_TYPE` missing in `SaasAdmin`, `companyName` in `utils/data.js`, `statusRender`/`folderCreateRender` in `Template/emailTemplate/create.js`), 9 `no-dupe-keys` (earlier duplicate removed so runtime value is unchanged), 2 `no-duplicate-case`, 5 `no-redeclare`, 6 `no-case-declarations`, 3 `no-inner-declarations`, 5 BOM regex literals; `--fix` for `no-extra-semi`/`no-useless-escape`. `npm run lint` script added. |
| 2026-09-04 | B.2 jest split | done | `jest.config.js` → projects `unit` (`tests/*.test.js`, 87 suites) + `conventions` (`tests/conventions/*.test.js`: `v2-guard`, trimmed `naming-conventions` (19 legacy-passing rules kept, 9 permanently-failing ones dropped), `unused-components` ratchet over `unused-components.baseline.json`). `npm test`: **90 suites, 1191 tests, all green**. `test:unit`/`test:conventions` scripts; `test:naming` removed. Ignore patterns anchored to `<rootDir>` (the bare `/.claude/` pattern silently ignored every test when the checkout lives under `.claude/worktrees/`). |
| 2026-09-04 | B.2 vitest | done | `frontend/vitest.config.js` (jsdom, `@` alias, `@pageContent` alias, `require("@/assets/…")` pre-transform), `frontend/tests/setup.js` (i18n plugin, `$t`, `$userId/$companyId/$clientWidth/$socket` provides, toast mock, IntersectionObserver shim). Specs: `TaskDetailPanel` (done checkbox → `updateStatus` with the `close` status), `ProjectsListPage` (row click → `push({name:'Project'})`), `CreateChannelSidebar` (empty name → no request), `useProjectHealth` (6 cases). `npx vitest run`: **4 files, 9 tests green**. `frontend/package.json` gains `"test": "vitest run"`; devDeps vitest 2, @vue/test-utils 2, jsdom 24, @vitejs/plugin-vue 5. |
| 2026-09-04 | B.2 CI | done | `.github/workflows/ci.yml` on PR/push to `beta`/`main`/`staging`, Node 20: backend `npm ci && npm run lint && npm test`; frontend `npm ci && npm run lint -- --no-fix && npm test && npm run build` with placeholder `VUE_APP_*`; `firebaseInit.js` skips `initializeApp` for a missing/placeholder key (`fcmToken` already catches the missing app). `main.yml`: setup-node 22 → 20; trigger left on `staging` because the job hard-resets the VPS to `origin/staging` (switching the trigger to `main` would deploy staging code on main pushes) — flagged for the integrator. Local build with `VUE_APP_APIKEY=placeholder`: exit 0. |
| 2026-09-04 | B.3 dead code | done | Four commits, frontend build green after each (exit 0 ×3 + installer commit touches no frontend). Group 1 task/filter/timesheet/import: 53 files, −11,289 lines (27 SFCs incl. the cascade orphans `TabListHeader/Item`, `TimeLog` view/table/rows, `AddTimeLog`, `ViewTimelogDetail`). Group 2 project/template/settings + `plugins/dashboard-old` (2,355 ln): 32 files, −5,038 lines. Group 3 chat: 9 files, −2,238 lines. Installer: `installation/` (100 files), `installation.js`, `check-version.js`, Dockerfile `COPY`, `.dockerignore`/`.gitignore` entries, `index.js` static mount, `scripts/dev.js` wizard stage/constants, `basic-install`/`check-version` scripts: 107 files, −53,421 lines. `node scripts/unused-components.js` = **0** (was 42); orphan `.js`/`.css` scan under `components/` = 0; `tests/conventions/unused-components.test.js` now asserts `[]`. `grep -rn "dashboard-old\|installation" frontend/src index.js Dockerfile` = 0; `scripts/dev.js` keeps 9 prose/console lines that mention the old wizard (no code references) — reworded by A.1 with the new setup flow. `tests/first-run-checklist.test.js` "wizard offers the options the backend understands" now reads `views/Setup/SetupWizard.vue` and skips until A.1 lands it (1 skipped). `npm test`: 90 suites, 1189 passed + 1 skipped. |
| 2026-09-04 | B.4 tenant scoping | done | `Config/tenant.js`: `tenantOf(req)` (header → params → query → body, ObjectId-validated, checked against `req.aud` comma-list/PAT single value, throws `TenantError` with `statusCode 403`) and `tenantDb(req)` → `(mongoObj, method) => MongoDbCrudOpration(companyId, …)`. ESLint override on `Modules/**/controller*.js`: `no-restricted-syntax` for a literal (other than `'global'`) or `undefined` first argument to `MongoDbCrudOpration` — 0 hits today, synthetic stdin check flags 2/2. Ratchet: `tests/conventions/tenant-scoping.test.js` over `tenant-scoping.baseline.json` (251 `req.body/req.query` tenant reads in 56 files; new files must be 0, per-file counts may only fall, baseline must be lowered when a module is cleaned). Unit: `tests/tenant.test.js` (6 cases). Adopted in `Modules/Pages/controller.js` (9 handlers) and `Modules/Webhooks/controller.js` (5); every handler's `catch` already answers `{status:false, statusText}`. `npm run lint`: 0 errors / 481 warnings. |
| 2026-09-04 | B.5 observability | done | `Config/requestContext.js` (AsyncLocalStorage), `Config/requestLog.js` (`X-Request-Id` assigned or echoed when well-formed; one `id method url status ms uid aud` line on `finish`; skips `/health`, `/socket.io/`, static paths) mounted before the body parsers in `index.js`; `loggerConfig.js` prefixes `[rid]` inside a request; `Config/errorHandler.js` after `spaFallback` (logs stack + id, body `{status:false, statusText, requestId}`, 500/`err.statusCode` only when `wantsStatusCodes`); `uncaughtException`/`unhandledRejection` now go through winston; `Config/respond.js` `ok/fail/asyncHandler` (`fail` writes `statusCode` so `strictStatus.inferStatus` maps it) adopted in the 14 Pages/Webhooks `catch` blocks. Tests: `request-log` (4), `error-handler` (4), `respond` (3) + `strict-status`/`spa-fallback` still green (21 tests). Smoke: `logger.error` inside `requestContext.run({id:'smoke-rid'})` → `error-<date>.log` line `… error: [smoke-rid] forced failure …`. Lint 0 errors / 480 warnings. |
| 2026-09-04 | B.6 project tree | done (list/folder creation depends on C.2) | `views/Projects/composables/useProjectTree.js` (`loadSprintFolderData`, `selectProject`, `resolveRouteProject`, `sprintLoading`; watches `projectData/sprints|folders`, `projects`, `route.params.id`) replaces `ProjectListing.vue` (693) + `ProjectListComponent.vue` (396); `ProjectHeader.vue` gets a native `<select class="ph2__switch">` project switcher (`projects` prop, `select-project` emit; label reuses `ProjectsV2.title`, no new locale keys). `Projects.vue`: tree block, `sidebarArrowIcon`, `responseWidth`, `projectList` ref, the ≤1300px drawer block and the three tour provides removed; AI creators call `loadSprintFolderData`/`selectProject`. `style.css`: `.section-left` gone, `.section-right` 100%, 1300px override dropped. `useProjectTour.js` deleted; `Tour/helper.js` 534 → 254 lines (seven legacy driver branches + their next/prev branches and six step JSONs removed; `isTaskTour` and the shell tour keep working; `TourComponet.vue` untouched for C.5). Cascade orphans removed: `ProjectFilter/` (4 SFCs), `ProjectsListing/style.css`. `ProjectTaskTypeForm.vue` injects the tour hook with a `null` default (one-line, unowned file). Verification: `grep -rn "ProjectListing\|section-left\|responseWidth\|projectList\." frontend/src` = 0 apart from unrelated local `projectList` refs in `views/Timesheet/*`, `App.vue`, `customFieldList.vue`, `store/ProjectData/actions.js` (a `payload.projectList` param); `node scripts/unused-components.js` = 0; frontend lint clean; vitest 5 files / 12 tests (new `useProjectTree.spec.js`: routed project, unknown-id fallback + toast + route rewrite, default-tab selection); build exit 0. **`NewInProjectMenu.vue` (C.2) is absent in this worktree, so creating a list or folder from the project header depends on C.2 landing.** Browser checks for the integrator below. |
| 2026-09-04 | B.7 i18n namespaces | done | `scripts/i18n-rename-namespace.js <From> <To>` / `--all`: merges a `XV2` block into its `X` twin in `en.js` (line-based, multi-line values kept, V2 value wins on a shared key) or renames the block, then rewrites every quoted `XV2.` prefix under `frontend/src` (t/te/$t and key strings such as `cardCatalog.js` labels). Run once: 26 namespaces — 10 merged (Auth, Home, Chat, Inbox, Settings, Audit, Members, Projects, Billing, Category), 16 renamed; 2,915 references in 137 files. Shared keys that differed (8, V2 copy kept): `Auth.create_account` "Create account"→"Create your account", `Auth.confirm_password`, `Home.today` "Today"→"today", `Inbox.mark_all_read`, `Settings.save_changes`, `Audit.title`, `Projects.close_project` (casing), `Billing.due_on` "Due on"→"DUE {date}" (no legacy caller). `grep -c "V2: {" en.js` = **0**; quoted `V2.` strings in src/tests = 0; `en.js` 133 → 124 namespaces and still parses. Static-key audit found 16 real gaps: 6 caller typos fixed (`ViewList.to_unlock_list_view`, `ProjectDetails.description`, `Toast.queue_list_not_updated` ×2, `Auth.loading`, `Toast.Start_and_Due_date_not_updated`) and 10 keys added to `en.js` (additive: `Time.loading/when_today/when_yesterday/when_custom/back_to_summary`, `Permissions.Write`, `Tour.Tag_name_required`, `dashboardCard.task_type`, `Toast.Assignee_not_updated`, new `Common.select_task/select_all_tasks`). `tests/conventions/i18n-namespaces.test.js` (+ `locale-keys.js` helper): no `*V2` namespace, no quoted `V2.` reference, every static `t('A.b')` resolves (6,475 keys scanned; `_`/`.`-terminated prefixes resolve by prefix). Other locales untouched (`fallbackLocale: en`). `npm test`: 96 suites, 1217 passed + 1 skipped; frontend lint clean; vitest 12/12. **Integrator: A/C branches still add `SetupV2`/`InstanceV2`/`AppsV2`/`TrashV2`/`HomeV2` keys — after merging them run `node scripts/i18n-rename-namespace.js --all` once; the convention test fails until then.** |
| 2026-09-04 | B.8 docs from code | done | `scripts/env-doc.js` (+ `scripts/env-doc.meta.json`, 150 keys with group/description/required/secret/default) scans `process.env.X`, `process.env['X']` and the `env = process.env` alias form across the backend and `VUE_APP_*` across `frontend/src`, `vue.config.js`, `public`; writes `docs/ENV.md` (124 backend + 24 frontend variables in 15 groups), a generated block at the end of `.env.example` (the hand-written head stays authoritative; 41 previously undocumented backend variables now listed with defaults) and a new `frontend/.env.example`. `node scripts/env-doc.js --check` = "env docs are in sync" (used-but-undescribed 41 → 0, described-but-unread 10 → 0 — 8 were read through `env.X` aliases the old audit missed, 2 (`MONGO_CLOUD_URL`, `LLM_MAX_TOKENS_CLARIFY`) are marked `external`); `tests/conventions/env-doc.test.js` wraps it. `Modules/Auth/routes2.js` folded into `routes.js` as `initSignup(app)` (no name clashes, same registration order); `find Modules -name routes2.js` = 0 and the naming test no longer carries an exception. Docs: new `docs/DEVELOPER-GUIDE.md` (run, test, tenant/respond/observability, add a module, add a view, env, locale keys); `.claude/ARCHITECTURE.md` (per-company database, request pipeline, errors, multi-tenancy rewritten); `.claude/CONVENTIONS.md` (routes.js + init.js + guard entry, asyncHandler/respond, tenantOf/tenantDb rule, new Tests and Locale-keys sections); `.claude/FOLDER-STRUCTURE.md` (Modules subtree regenerated from `ls Modules`: 98 dirs + 4 files; `installation/`, `installation.js`, `check-version.js` entries gone); `CONTRIBUTING.md` and `BRANCHING.md` (`beta` → `staging` → `main`, CI gate); `README.md` setup section (in-app `/setup`, no wizard, gate commands). `generate-env.js` untouched (A.4 deletes it). Final gate: `npm test` 97 suites / 1219 passed + 1 skipped; `npm run lint` 0 errors / 480 warnings; vitest 5 files / 12 tests; frontend lint clean. |
