# Progress — workstream A

| Date | Step | Result | Evidence |
|---|---|---|---|
| 2026-09-04 | A.1 first-run inside the app + Docker/bare-metal boot | done | `Config/applyEnv.js` (`applyEnvMap`, `.env` guarded by `existsSync`, real env wins over `.env`), `Config/instanceState.js`; `Modules/CheckInstallStep` → `Modules/Setup` (`GET /api/v2/setup/status`, `POST /api/v2/setup/complete` 400/409/503, SSE `/api/v2/setup/events/:id`), probes moved to `Modules/Instance/probes.js`, `installationSteps.json`/`persistEnvValue`/rebuild step deleted; `/health` → JSON 200/503; Dockerfile copies `cron.js CHANGELOG.md docs/`, creates `storage log backups`, `STORAGE_TYPE=server` default, HEALTHCHECK on `/health`; compose `MONGODB_URL` without a db name (the app appends one per company) and volumes on `/app/storage` (was shadowing `common-storage/*.js`), `/app/log`, `/app/backups`; cron gated on `CRON_ENABLED !== 'false'` with `recordCronRun`; `scripts/dev.js` without the wizard build; `.env.example` gains `CRON_ENABLED MIGRATIONS_AUTO HELMET_ENABLED BACKUP_DIR INSTANCE_ADMIN_KEY`, blank `SOCKETIO_ADMIN_*`. Frontend: `views/Setup/SetupWizard.vue` at `#/setup`, `router/setupStatus.js` memoised guard in `router/index.js` (fresh → `/setup`, installed → `/login`), `SetupV2` locale. Tests `setup-status`, `health` (+ `first-run-checklist` repointed, `v2-guard` allow-lists `/api/v2/setup`): 93 suites / 1209 tests. Boot `PORT=4101 node index.js`: `curl :4101/health` → `{"status":"ok","db":{"ok":true,...}}` [200]; `setup/status` → `installed:true` [200]; `setup/complete` → 409 on the dev DB, 400 with field errors on `{}`. |
| 2026-09-04 | A.3 migrations runner + boot hook | done | `migrations/index.js` (`listMigrations`, `planRuns`, `runMigrations` with `global.schema_versions` records `{_id, appliedAt, durationMs, appVersion, ok, error, companies}` and a `__lock {owner, expiresAt}` row, `runMigrationsAtBoot` before `listen`, `MIGRATIONS_AUTO=false` reads status only), `migrations/store.js` (Mongo + in-memory), `001-baseline`, `002-global-seeds` (only when `global_custom_fields` is empty), `003-permission-catalogue` (`repairCompanyRules` per company; `PERMISSION_RECONCILE` env + lazy request-path repair removed), `004-task-type-icons` (`migrations/lib/taskTypeIcons.js`), `005-main-chat-flag`, `006-formula-rollup-types`; `scripts/migrate.js status|up` (`npm run migrate`, `migrate:status`); the three ad-hoc scripts deleted; `SCHEMA_VERSIONS`/`INSTANCE_SETTINGS` in `Config/schemaType.js`, `Config/collections.js`, `utils/mongo-handler/{schema,createSchema,mongoQueries}.js`; `.gitignore` no longer ignores `migrations`; Dockerfile copies `migrations/`. Tests `migrations-runner` (15) + `permission-reconcile-rules` rewritten: 94 suites / 1218 tests. E2E on throwaway `zz_a_test_co1` (dropped after): run 1 applied 001/003/004/005 with per-company records (`rules: 99`, `keyFixes: 1, merged: 1, tasksRewritten: 1`, `repaired: 1`), run 2 applied nothing; `npm run migrate:status` on dev → 6 pending; boot with `MIGRATIONS_AUTO=false` → `/health` `migrationsPending: 6` [200]. |
| 2026-09-04 | A.2 Instance console | done | Backend `Modules/Instance/` (absorbs `SaasAdmin`): `guard.js` (`isProductOwner` session or `INSTANCE_ADMIN_KEY` header; API tokens refused), `settingsCatalog.js` (45 keys in 7 groups, secrets/restart/public flags, `validateSettings`, `describeSettings`), `Config/instanceSettings.js` (`global.instance_settings` doc, AES-256-GCM secrets keyed from `JWT_SECRET`, env keys locked at boot, `loadInstanceSettings` before modules load, `saveInstanceSettings` → `applyEnvMap` + cache clear + `INSTANCE_SETTINGS_UPDATED` emit), endpoints `GET/PUT /api/v2/instance/settings`, `POST /settings/test`, `GET /health` (db latency, storage writable+free space, mail, agenda pending/failed, cron last runs via `recordCronRun`, migrations, last backup, readiness), `POST /maintenance`, `GET /public-config` (public, cached 60s), `GET /upgrade` (GitHub latest cached 6h, changelog releases newer than current via the exported parser, migrations), `POST /migrations/run`, `GET /logs?file=error|combined|track&lines=`, `/logs/files`, `/logs/download` (basename allow-list), backups (`POST /backups` streams `.tar.gz` with `manifest.json` + `<db>/<coll>.jsonl` EJSON + optional `storage/`, `GET`, `/:name/manifest`, `/:name/download`, `POST /:name/restore` typed confirmation + safety backup + maintenance + drop/insertMany + migrations + cache flush + connection reset, `DELETE`), `stats`, `companies`, `audit-export`. `tar-stream` declared in package.json; `/backups` gitignored. Frontend: `config/publicConfig.js` + `brandSettingTab/setPublicConfig` replace `VUE_APP_IS_*`/`VUE_APP_*_CLIENT_ID` reads in Login, Invitation, oAuthProviders, commonFunction, Google/GitHub/GitlabAuth, SsoSettings (Firebase keys stay build-time, noted in `firebaseConfig.js`); `views/Settings/Instance/{InstanceShell,InstanceHealth,InstanceSettings,InstanceBackups,InstanceUpgrade,InstanceLogs,InstanceStats}.vue` under `settings/instance/*`, nav group “Instance” in `SettingsShell.vue` gated by `GET /api/v2/instance/access`, Health reuses `SetupChecklist.vue` as the readiness card, every page links `ADMIN-GUIDE.md#anchor`; `InstanceV2` locale. Tests `instance-settings-catalog` (10), `logs-path` (7), `backup-manifest` (6): 97 suites / 1239 tests. curl on :4101 with `adminkey`: public-config 200 without auth; access 401 without / 200 with key; settings GET 45 rows, env key `source:"env", locked:true`; PUT unknown+bad → 400 `{JWT_SECRET:"unknown", NODEMAILER_PORT:"number"}`; PUT `TURN_USERNAME/TURN_PASSWORD` → applied, GET shows `{set:true}`/`source:"saved"`, Mongo doc holds `enc:v1:…`; test storage → writable; health → db 8ms, storage free space, agenda 0/0, migrations pending 6; upgrade → latest v14.35.0 from GitHub, `updateAvailable:false`; logs tail 3 lines; `logs/download?name=../.env` → 404; backup create 112 KB / 2 dbs / 18 global collections, `tar -tzf` shows `manifest.json` first; restore with wrong confirm → 400; delete → 200. |
