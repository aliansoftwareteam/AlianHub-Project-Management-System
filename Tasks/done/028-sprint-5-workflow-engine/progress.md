# Progress: Sprint 5 — the workflow engine

## Checklist
One pull request per step; tick with the merge commit.

- [x] Step 1: Workflow runs and step runs as the unit of everything, generalised from the automation runner: dependencies, r — #669 `10b85281` (build 144): the two collections with migration 016, the compare-and-set claim, leases with fencing tokens, ready-set scheduling, per-tenant claim counts, deterministic versus transient retries, idempotency keys on audit rows
- [x] Step 2: Step types: agent run, tool call, human approval with owner, deadline and escalation, fan-out and fan-in, cond — #674 `76c0e9a9` (build 149): the eight types behind `executors.js`, approvals as rows of their own, bounded loops, and `schedule.due` as an entry in the automation trigger catalogue
- [x] Step 3: Agent runs become step executors; user-started runs go through the queue like rule-started ones already do; th — #675 `8dec88cc` (build 151): `agent_run` through the existing runner, `POST /api/v2/agents/runs` wrapped in a one-node workflow, and `/api/v2/workflows` with an idempotency key on start and per-step retry, skip, resume and compensate
- [x] Step 4: Dispatch rides the durable queue with typed results validated at each edge; deadline and budget shrink per hop — #678 `f79b1aef` (build 154): `typed.js` holds an executor to its output before it is written and a consumer to the producer's contract, `hop.allow` grants each step what is left, and the depth guard reaches `runs.canStart` as the counter sprint 1 already threaded
- [x] Step 5: (added) The hourly run limit that is stored and never enforced becomes the loop's admission control, so the sc — #677 `eac0c788` (build 152): the smallest of the step's, the rule's and the instance's `maxRunsPerHour`, counted per company and agent over a rolling hour, stopping a loop with `stoppedBy: 'run_limit'` rather than failing it
- [x] Interface: Workflow run → lineage (new) — #682 `cac6ec66` (build 157); owner and member sweep still to record
- [x] Interface: Workflow builder (new) — #681 `540a2e05` (build 156); owner and member sweep still to record
- [x] Interface: Workflow run view (new) — #680 `9e1813f0` (build 155); owner and member sweep still to record
- [x] Interface: AI Inbox → approval steps (extend) — #682 `cac6ec66` (build 157); owner and member sweep still to record
- [x] Interface: Workflow run → iteration counter (new) — #680 `9e1813f0` (build 155); owner and member sweep still to record
- [x] Interface: Workflow run → failed step (new) — #680 `9e1813f0` (build 155); owner and member sweep still to record
- [x] Defects closed: #17 — #677 `eac0c788` (build 152): the stored hourly run limit is read and enforced
- [x] Exit gate met and gates green — #683 `b7e9f9fe` (build 158); the owner and member sweeps of the six screens are still to record

## Log
| Date | Entry |
|---|---|
| 2026-09-10 | Filed from `docs/AI-PLATFORM-ARCHITECTURE.md` (sprint 5) |
| 2026-09-12 | Started. Step 1 (durable workflow and step runs, dependency scheduling, per-tenant claim counts, heartbeat leases with fencing, deterministic versus transient retries, idempotency keys) is in progress on `feat/sprint-5-workflow-runs`. Steps 2–5 wait for it. |
| 2026-09-12 | Steps 1–3 merged: #669 `10b85281` (build 144), #674 `76c0e9a9` (build 149), #675 `8dec88cc` (build 151). Everything sits behind `WORKFLOW_ENGINE`, off by default, where an existing automation rule becomes a one-node workflow and the Automations page is unchanged — what moves is the bookkeeping: the step is claimed under a lease, its failure is classified once, and its retry is scheduled by the engine rather than by the runner. A waiting step throws rather than holding a worker, so an approval that takes days and a timer a week out cost nothing while they wait. |
| 2026-09-12 | Steps 5 and 4 merged in that order: #677 `eac0c788` (build 152) makes `automationRules.limits.maxRunsPerHour` — written on every v2 rule and read by nothing — the loop's admission control, which is defect #17 closed; #678 `f79b1aef` (build 154) types the result at each edge and shrinks deadline and budget per hop. A step that asks for more than remains is refused rather than clamped, and the run goes to `blocked` naming the step, because a step given a tenth of the deadline it says it needs usually fails part-way through, having already spent the money. |
| 2026-09-12 | The six interface rows merged in three pull requests: #680 `9e1813f0` (build 155) is the run view and carries three of them — the step graph with status, duration and cost, the iteration-and-budget counter with its stop control, and the failed-step card with the error, whether it was deterministic, the attempts and the one control that applies; #681 `540a2e05` (build 156) is the builder, every slot drawn from `GET /step-types` so a type the engine does not know cannot be composed, saved turned off, with `POST /dry-run` planning against a real task and writing nothing; #682 `cac6ec66` (build 157) puts workflow approvals in the AI Inbox with owner, deadline, escalation and reassign, and adds the lineage view. |
| 2026-09-12 | Exit gate passed in #683 `b7e9f9fe` (build 158): a fifteen-step workflow killed at step eleven resumes at eleven, re-executes nothing behind it and writes every effect exactly once; an approval step waits, records a reassignment, escalates on its delay and resumes on the decision after the handover. Writing the gate found the hole it was meant to prove was absent — `scheduler.readySet` offered only `pending` steps, so a run killed mid-step sat there until a person pressed resume. A step is now claimable when it is pending **or** running under a lapsed lease, which is the other half of the mechanism `store.claimStep` already had. |
| 2026-09-12 | Not done, and not recorded as done: the second acceptance bullet asks for every interface row to be swept by the owner and by a member, and no sweep of the six workflow screens exists. A browser check of the builder did turn up one navigation problem, filed as item 58 in task 034's `followups.md` — the builder is reachable only from the AI sidebar's Workflows entry (owners and admins), it is the one AI-section screen that does not render that sidebar, and the global rail's AI entry matches route names beginning with `Ai`, which `WorkflowBuilder`, `WorkflowRun` and `WorkflowLineage` do not. It is a UI change rather than a docs one, so it is not fixed here. |

## Last step
Complete: five steps merged, all six interface rows shipped, defect #17 closed, and the kill-and-resume gate passed in #683 (build 158). Moved to done. Still open: the owner and member sweeps of the six workflow screens, and the navigation follow-up (item 58 in task 034).
